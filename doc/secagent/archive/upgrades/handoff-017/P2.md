# ===== NEXT PROMPT (P2) =====

你是 SilkSecAgent v5 的 DSH 升级执行 agent。本会话执行「0.1.7 升级链」第 2/8 步：**P2 · U1a 版本解放与工具链修复**。

# 恒定段（每一步都必须随 NEXT PROMPT 原样携带）

## 授权（用户已一次性授予，严禁逐步确认）
允许无需回问直接执行：仓库读写/本地测试/git commit+push；`spool exec/query/dispatch`；`rsync -a bundles/dsh/ /opt/SilkSpool/bundles/dsh/`；`spool bundle dsh setup csai`；`spool restart csai silksecagent`；在 `/opt/silkspool/dsh-upgrades/` 下创建/清理升级工作目录；冻结/切换/回滚工具（P5 起）；`bus.prune` 维护；观察期重启与巡检。
**U3 生产切换预授权**：P5 全部门禁为绿即自动执行 P6，不再回问；失败立即回滚并按硬停止输出。
**红线**：一切远程操作走 PATH 中的 `spool`（禁止直连 SSH/curl 操作远程 Docker）；禁止 `docker compose down`（尤其 n8n 禁 `-v`）；禁止 `git add -f`；不提交 keys/config.ini 等被忽略文件；改 bundle 模板后必须先 rsync 运行时副本再 `spool bundle dsh setup csai`；Session 迁移必须先有完整冻结点 + 恢复预演。
**硬停止条件（仅这 4 类，遇到即停下并输出 CHAIN BLOCKED）**：① P5 任一门禁失败且当前步无法修复；② 生产不变量出现白名单外的非预期差异；③ 需要超出预期的停机且无法自动恢复；④ 用户喊停。其余一律自行决策继续。

## 基线（已定，不要重新论证）
- 目标版本：DSH **0.1.7-rc.2**（GitHub commit `477b4f420553`，release 2026-09-24T14:10:21Z；npm dist-tag `next`）。若期间上游出现更新 rc，只在记录中注明，**不切换目标**（除非用户明确指示）。截至 P1 无 0.1.7-rc.3。
- 现役生产：0.1.5-rc.2，csai `/opt/silkspool/dsh/`，web 服务 `silksecagent`（edge `silksecagent-edge`）。
- 已定决策：官方 `dsh-schedule` 不接入；dsh-bill 升版并重写计费补丁；保留窗口「7 天或 2 万行取大」；自研 interval 下限保持 300s。
- 执行真相源：`doc/secagent/25-dsh-0.1.7-upgrade-and-scale-2026-09-26.md`（§一 P0-1..P0-6 与 U0–U4 为准）。
- 流程/格式范例：`doc/secagent/archive/upgrades/TEMPLATE.md`、`2026-09-12-dsh-0.1.5-rc.2-plan.md`、`-record.md`、`HANDOFF-u3-switch.md`。
- 升级工具位于 `bundles/dsh/templates/`（setup.sh / dsh-upgrade*.py|sh / dsh-session-*.mjs / dsh-version-watch.sh / dsh-runtime-compat.py）。

## 步骤地图（恒定，用于生成下一步）
- P1 U0 预检与建账 → P2 U1a 版本解放与工具链修复 → P3 U1b Session V4 + Agent Preset → P4 U1c settings + 第三方插件 + 部署链 → P5 U2 隔离预演（全绿自动放行）→ P6 U3 生产切换 → P7 U4a 巡检 1 → P8 U4b 巡检 2 + 关账（CHAIN END）。

## STATE 模板
```yaml
current_step: P2
target_version: 0.1.7-rc.2
target_commit: 477b4f420553
current_prod_version: 0.1.5-rc.2
upgrade_dir: /opt/silkspool/dsh-upgrades/20260926-017
candidate_dir: ''
freeze_state: ''
release_dir: ''
observation_until: ''
gates: {}
open_issues: []
last_commit: ''
```

## NEXT PROMPT 递归规则（每步结束必须执行）
1. 每步结束输出**恰好一个** `===== NEXT PROMPT (P<n>) =====` 围栏代码块；内容自包含（新会话零上下文可用）：角色、恒定段、更新后的 STATE、上一步完成事实（时间/产物/SHA/证据）、本步任务、验收门禁、记录要求、本递归规则。
2. 同步把该 prompt 写入 `doc/secagent/archive/upgrades/handoff-017/P<n>.md`。
3. P8 关账成功输出 `===== CHAIN END =====`；任一硬停止输出 `===== CHAIN BLOCKED =====` + 失败事实 + 恢复建议（仍属终止）。
4. 每步开头先校验 STATE 与前置产物（文件/SHA/服务状态），不一致按硬停止处理。
5. 记录纪律：证据 = 时间戳 + 命令 + 退出码 + 关键输出 + SHA-256；**不填未验证的「通过」**，失败据实记录并给归因。

# 本会话 STEP STATE（P2 开始前，来自 P1）

```yaml
current_step: P2
target_version: 0.1.7-rc.2
target_commit: 477b4f420553
current_prod_version: 0.1.5-rc.2
upgrade_dir: /opt/silkspool/dsh-upgrades/20260926-017
candidate_dir: ''
freeze_state: ''
release_dir: ''
observation_until: ''
gates: {}
open_issues:
  - freeze 工具调度器摘要路径失配：dsh-upgrade-freeze.py:111 指向已删除的 plugins/sec-suite/scheduler.js；现 scheduler.lock 写入者为 plugins/sec-domain-task/index.js（sha256 6b21fb7e5458757531b80756c6e2ca69ffadd3c2e24e9e1dfef9575ed5140161）；freeze-config-current.json scheduler_sha256=6c9b93e9… 亦过期（P2 适配，U2 冻结预演验证）
  - dsh-upgrade-candidate.py:13,132 引用不存在的 dsh-plugin-sec-suite.scheduler.js（P2 修复）
  - dsh-upgrade-runtime-smoke.py:127 override id "web-app" 应为 "web-runtime"（P2 修复）
  - 工具单测在非 root 环境 freeze/release 有 3+1 项环境性失败（P2 以 root/目标机复核）
  - 官方 schedule 保持不接入（决策，非缺陷）
last_commit: c1986a4  # P1 建账提交；纯文档勘误后继可接受
```

# P1 已完成事实（2026-09-26，北京时间 21:42–22:05 / UTC 13:42–14:05）

- **生产只读预检**：`silksecagent` active/running、MainPID **635167**、NRestarts=0、15 域注册、journal 0 fatal/panic；`sec-v5-accept.sh --ui-headless` **PASS=80 FAIL=0**；outbox total 121405（delivered 121396 / pending 1 / dead 8）；`scheduler.lock` pid=635167 刷新中；磁盘可用 824G；sessions 518M/1830 文件。
- **上游锁定**：`@deepseek-ai/dsh@0.1.7-rc.2`（commit `477b4f420553`）主包 + 9 关键子包（base/web-app/headless/agent-preset/settings/session-format-v3-to-v4/schedule/plugin-manager/workflow-ptc）sha512 与 npm `dist.integrity` 复算 **ALL_MATCH**，sha256 已锁入 state；旧包 `dsh-agent-presets`/`dsh-settings-file` 在 0.1.7-rc.2 = 404。
- **工具链盘点**：12 处 `0.1.5-rc.2` 硬编码清单（record §5.1）；2 个既存 bug 确认（record §5.2）；**新发现 freeze 调度器摘要失配**（record §5.4）；模板一致性 33/33。
- **工作目录与证据**：`/opt/silkspool/dsh-upgrades/20260926-017/precheck/` 4 份证据（SHA-256：`precheck-csai.txt ef9e6cf5…`、`precheck-packages.json 4ce261fe…`、`precheck-thirdparty.json cbc72bc9…`、`precheck-toolchain-hardcodes.txt d6ba0adb…`，远端与本地一致）。
- **建账已提交**：[plan](../2026-09-26-dsh-0.1.7-rc.2-plan.md)、[record](../2026-09-26-dsh-0.1.7-rc.2-record.md)、[state](../2026-09-26-dsh-0.1.7-rc.2-state.md)、[handoff-017/P1.md](P1.md)、PROGRESS 一行；commit `chore(secagent): DSH 0.1.7 升级链启动——P1 预检与建账`。

# 本步任务（P2 · U1a 版本解放与工具链修复）

> 目标：让升级工具链同时认识「源版本 0.1.5-rc.2」与「目标版本 0.1.7-rc.2」，修复两个既存 bug 与 P1 新发现的 freeze 摘要失配。**本步不改生产、不重启、不运行 setup、不部署 bundle**；全部改动在仓库 `bundles/dsh/templates/` 内完成并本地/远程隔离测试。

1. **前置校验**：核对 STATE 与实际（`git log` HEAD、`upgrade_dir` 与 4 份 precheck 证据 SHA、`spool service csai status` 生产健康）；不一致按硬停止处理。
2. **P0-1 版本解放（参数化，保持向后兼容）**：
   - `setup.sh:12`：目标版本参数化（env/参数，如 `DSH_TARGET_VERSION`），默认值在当前阶段仍为 0.1.5-rc.2（P6 切换时改默认）；保留跨版本拒绝语义（只允许 0.1.5-rc.2 → 0.1.7-rc.2 的受控组合，未知组合拒绝）。
   - `dsh-upgrade.sh:14,19`：参数化为「源版本 → 目标版本」对（usage/校验同步）。
   - `dsh-upgrade-candidate.py:12,48`：`VERSION` 参数化；**同时修复既存 bug #1**（删除 `dsh-plugin-sec-suite.scheduler.js` 引用，:13 列表与 :132 映射；指向现役 `dsh-plugin-sec-domain-task.js` 逻辑或不引用），并补回归测试。
   - `dsh-upgrade-egress.py:24`：版本校验参数化（读候选 app 版本 + 允许源/目标集合；读 settings.yaml 改生效配置属 P4，本步不做）。
   - `dsh-upgrade-release.py:31`：新旧 pin 参数化（Session V4 发布语义属 P3，本步不做）。
   - `dsh-runtime-compat.py:151,192,209`：按目标版本分支；0.1.5 路径行为**逐字不变**；0.1.7 分支建立显式结构（patch_settings/patch_theme 的删除与替换属 P4，本步可留 raise/标注明确失败而非静默）。
   - `dsh-session-*.mjs`（`dsh-session-recover.mjs:34`、`dsh-session-recovery.mjs:17`、`dsh-session-rehearsal.mjs:74`）：目标版本参数化（V4 断言改造属 P3，本步只解放版本常量）。
   - `data-seed/scripts/dsh-version-watch.sh:8`：`KNOWN` 参数化（默认保持现役；P6 部署时改 0.1.7-rc.2）。
   - 测试 fixture 跟随（`dsh-upgrade-installer.test.py:44,57,67`、`dsh-plugin-sec-suite.persona.test.py`、`dsh-session-recovery.test.mjs`）：新增 0.1.7-rc.2 用例，保留旧版本用例。
   - 逐文件记录「旧→新」语义，保证 `grep -n "0.1.5-rc.2"` 的每处剩余命中都有明确理由（源版本常量/向后兼容/旧版 fixture）。
3. **freeze 调度器摘要适配（P1 新发现）**：`dsh-upgrade-freeze.py:107-115` 的模块路径从已删除的 `plugins/sec-suite/scheduler.js` 改为现役 `plugins/sec-domain-task/index.js`（当前 sha256 `6b21fb7e5458757531b80756c6e2ca69ffadd3c2e24e9e1dfef9575ed5140161`，需在测试中可注入/参数化，不写死单一构建）；更新 `dsh-upgrade-freeze.test.py` 与（生成/校验端）freeze 配置的期望摘要逻辑；为 U2 冻结预演留出「配置生成」路径（`freeze-config-current.json` 属运行态文件，P2 只改工具与测试，不改 csai 运行态配置）。
4. **既存 bug #2**：`dsh-upgrade-runtime-smoke.py:127` 的 `"web-app"` 改为 `"web-runtime"`，补断言（override 真的落到目标行、`openBrowser: False` 生效）。
5. **工具单测（本步验收核心）**：运行并记录
   - `python3 dsh-upgrade-*.test.py`（7 个：freeze/installer/invariants/local-client/maintenance/release/snapshot）
   - `node --test dsh-upgrade-rpc.test.mjs`、`node --test dsh-session-recovery.test.mjs`（如可离线）
   - 非 root 环境的 freeze/release 失败项：在 csai `upgrade_dir` 内以 root/silkspool 正规环境复跑（工具经 base64/tar 传入 `upgrade_dir`，**不得覆盖 `/opt/silkspool/dsh/` 生产文件、不得重启服务**），证明失败是环境性而非代码性；若为代码性则在 P2 修复。
   - 结果写入 record（时间/命令/退出码/通过数），未通过不得写「通过」。
6. **记录与提交**：
   - 更新 `record.md`：追加「P2 执行记录」章节（改动清单、旧→新对照、测试结果、SHA-256）；更新 §5 硬编码清单为「已参数化（P2）」。
   - 更新 `state.md`：`gates.toolchain_version_liberation: pass`、`open_issues` 移除已修项、`last_commit`。
   - 更新 `handoff-017/P1.md` 的 commit 行（可选）。
   - `git add` 指定文件（禁用 `-f`、禁用 `-A`），commit 信息：`feat(secagent): DSH 0.1.7 升级链 P2——P0-1 版本解放与工具链修复`（可多行说明既存 bug 与 freeze 适配）；`git push origin <当前分支>`。
7. **输出**：① 本步执行摘要与证据；② 风险/未决项；③ `===== NEXT PROMPT (P3) =====`（含更新 STATE；P3 任务：P0-2 Session 日志 V4 适配 + P0-3 Agent Preset 机制替换 + 相关单测/契约 + 提交），并写入 `handoff-017/P3.md`。

# 验收门禁（P2）

- **参数化完备**：`grep -rn "0.1.5-rc.2" bundles/dsh/templates/` 的每处命中都有理由说明；新增 `0.1.7-rc.2` 目标在工具单测中真实走通（fixture 级），不是只替换字符串。
- **向后兼容**：0.1.5 路径的既有单测行为不变（invariants 15、local-client 3、maintenance 6、snapshot 5、installer 3 等）。
- **两个既存 bug**：candidate 不再引用不存在文件；runtime-smoke override id 修正且有断言。
- **freeze 适配**：模块路径与期望摘要可注入；单测覆盖新路径。
- **零生产影响**：不重启、不 setup、不写 `/opt/silkspool/dsh/`、不改运行态 freeze 配置。
- **记录纪律**：所有「通过」都有 时间+命令+退出码+输出/SHA 支撑。

# 硬停止输出格式（如触发）

```
===== CHAIN BLOCKED =====
失败步：P2
事实：<时间/命令/退出码/关键输出>
影响：<生产是否受影响>
恢复建议：<具体回滚/修复路径>
```
