# ===== NEXT PROMPT (P3) =====

你是 SilkSecAgent v5 的 DSH 升级执行 agent。本会话执行「0.1.7 升级链」第 3/8 步：**P3 · U1b Session V4 适配 + Agent Preset 机制替换**。

# 恒定段（每一步都必须随 NEXT PROMPT 原样携带）

## 授权（用户已一次性授予，严禁逐步确认）
允许无需回问直接执行：仓库读写/本地测试/git commit+push；`spool exec/query/dispatch`；`rsync -a bundles/dsh/ /opt/SilkSpool/bundles/dsh/`；`spool bundle dsh setup csai`；`spool restart csai silksecagent`；在 `/opt/silkspool/dsh-upgrades/` 下创建/清理升级工作目录；冻结/切换/回滚工具（P5 起）；`bus.prune` 维护；观察期重启与巡检。
**U3 生产切换预授权**：P5 全部门禁为绿即自动执行 P6，不再回问；失败立即回滚并按硬停止输出。
**红线**：一切远程操作走 PATH 中的 `spool`（禁止直连 SSH/curl 操作远程 Docker）；禁止 `docker compose down`（尤其 n8n 禁 `-v`）；禁止 `git add -f`；不提交 keys/config.ini 等被忽略文件；改 bundle 模板后必须先 rsync 运行时副本再 `spool bundle dsh setup csai`；Session 迁移必须先有完整冻结点 + 恢复预演。
**硬停止条件（仅这 4 类，遇到即停下并输出 CHAIN BLOCKED）**：① P5 任一门禁失败且当前步无法修复；② 生产不变量出现白名单外的非预期差异；③ 需要超出预期的停机且无法自动恢复；④ 用户喊停。其余一律自行决策继续。

## 基线（已定，不要重新论证）
- 目标版本：DSH **0.1.7-rc.2**（GitHub commit `477b4f420553`，release 2026-09-24T14:10:21Z；npm dist-tag `next`）。若期间上游出现更新 rc，只在记录中注明，**不切换目标**（除非用户明确指示）。截至 P2 无 0.1.7-rc.3。
- 现役生产：0.1.5-rc.2，csai `/opt/silkspool/dsh/`，web 服务 `silksecagent`（edge `silksecagent-edge`）。
- 已定决策：官方 `dsh-schedule` 不接入；dsh-bill 升版并重写计费补丁；保留窗口「7 天或 2 万行取大」；自研 interval 下限保持 300s。
- 执行真相源：`doc/secagent/25-dsh-0.1.7-upgrade-and-scale-2026-09-26.md`（§一 P0-1..P0-6 与 U0–U4 为准）。
- 流程/格式范例：`doc/secagent/archive/upgrades/TEMPLATE.md`、`2026-09-12-dsh-0.1.5-rc.2-plan.md`、`-record.md`、`HANDOFF-u3-switch.md`。
- 升级工具位于 `bundles/dsh/templates/`（setup.sh / dsh-upgrade*.py|sh / dsh-session-*.mjs / dsh-version-watch.sh / dsh-runtime-compat.py）。

## 步骤地图（恒定，用于生成下一步）
- P1 U0 预检与建账 → P2 U1a 版本解放与工具链修复 → P3 U1b Session V4 + Agent Preset → P4 U1c settings + 第三方插件 + 部署链 → P5 U2 隔离预演（全绿自动放行）→ P6 U3 生产切换 → P7 U4a 巡检 1 → P8 U4b 巡检 2 + 关账（CHAIN END）。

## STATE 模板
```yaml
current_step: P3
target_version: 0.1.7-rc.2
target_commit: 477b4f420553
current_prod_version: 0.1.5-rc.2
upgrade_dir: /opt/silkspool/dsh-upgrades/20260926-017
candidate_dir: ''
freeze_state: ''
release_dir: ''
observation_until: ''
gates: {}
open_issues: []
last_commit: ''
```

## NEXT PROMPT 递归规则（每步结束必须执行）
1. 每步结束输出**恰好一个** `===== NEXT PROMPT (P<n>) =====` 围栏代码块；内容自包含（新会话零上下文可用）：角色、恒定段、更新后的 STATE、上一步完成事实（时间/产物/SHA/证据）、本步任务、验收门禁、记录要求、本递归规则。
2. 同步把该 prompt 写入 `doc/secagent/archive/upgrades/handoff-017/P<n>.md`。
3. P8 关账成功输出 `===== CHAIN END =====`；任一硬停止输出 `===== CHAIN BLOCKED =====` + 失败事实 + 恢复建议（仍属终止）。
4. 每步开头先校验 STATE 与前置产物（文件/SHA/服务状态），不一致按硬停止处理。
5. 记录纪律：证据 = 时间戳 + 命令 + 退出码 + 关键输出 + SHA-256；**不填未验证的「通过」**，失败据实记录并给归因。

# 本会话 STEP STATE（P3 开始前，来自 P2）

```yaml
current_step: P3
target_version: 0.1.7-rc.2
target_commit: 477b4f420553
current_prod_version: 0.1.5-rc.2
upgrade_dir: /opt/silkspool/dsh-upgrades/20260926-017
candidate_dir: ''
freeze_state: ''
release_dir: ''
observation_until: ''
gates:
  toolchain_version_liberation: pass
open_issues:
  - freeze 工具已改默认模块 plugins/sec-domain-task/index.js 且 scheduler_module 可注入；生产运行态 freeze-config-current.json（20260913-rc2/，scheduler_sha256=6c9b93e9… 指旧构建）未动 → U2 冻结前用 scheduler-digest 生成新配置（P5）
  - dsh-runtime-compat.py 0.1.7 分支显式 raise PENDING_017：patch_settings/patch_theme/patch_billing 按新运行产物重写属 P4
  - Session V4 断言/发布语义（P3）、Agent Preset 0.1.7 机制（P3）、settings/第三方插件/部署链（P4）未开始
  - 独立调用 release/maintenance/egress/session 工具须显式携带 DSH_TARGET_VERSION=0.1.7-rc.2 + DSH_OLD_VERSION=0.1.5-rc.2（dsh-upgrade.sh 自动导出）
  - 官方 schedule 保持不接入（决策，非缺陷）
last_commit: <P2 提交哈希，见 state.md / git log>
```

# P2 已完成事实（2026-09-26，北京时间 22:05–22:40 / UTC 14:05–14:40）

- **P0-1 版本解放（19 个文件改动 + 1 新增测试）**：受控组合白名单 `0.1.2-rc.1→0.1.5-rc.2`、`0.1.5-rc.2→0.1.7-rc.2`，未知组合落盘前拒绝；默认值保持 0.1.5（P6 切换默认）。关键设计：
  - `setup.sh`：`DSH_VERSION="${DSH_TARGET_VERSION:-0.1.5-rc.2}"` + 集合校验；
  - `dsh-upgrade.sh`：`--source-version`/`--version` 对 + `export DSH_TARGET_VERSION/DSH_OLD_VERSION`；
  - `dsh-upgrade-candidate.py`：版本从候选 app 派生，封存时把候选内 setup/watch/upgrade 默认值改写为「恢复点源版本→候选目标版本」，report 增 `source_version`；
  - `dsh-upgrade-release.py`：`VERSION/OLD_VERSION` 读 env + `require_supported_pair()` + pin 校验兼容参数化表达式；
  - `dsh-runtime-compat.py`：0.1.5 路径逐字不变，0.1.7 显式 `raise PENDING_017`（P4 重写 patch_settings/patch_theme/billing）；
  - session 四件套（read/recover/recovery/rehearsal）与 `dsh-version-watch.sh` 目标版本参数化（V4 语义未动，正是 P3 任务）。
- **两个既存 bug 修复**：candidate 删除已删除的 `sec-suite.scheduler.js` 引用（新增 `dsh-upgrade-candidate.test.py` 4 用例）；runtime-smoke override 改 `web-runtime` 且加「覆盖必须落到目标行 + `openBrowser=false` 真实生效」双重断言。
- **freeze 调度器适配**：默认模块 `plugins/sec-domain-task/index.js`，`scheduler_module` 可注入（绝对路径/`..` 拒绝）；新增 `scheduler-digest` 子命令；对生产复算 **6b21fb7e5458757531b80756c6e2ca69ffadd3c2e24e9e1dfef9575ed5140161**（= P1 记录值）；运行态 freeze 配置未动。
- **测试（全绿）**：本地 9 组 Python + node；csai root（`p2-tools/`，零生产写入）freeze 9 / release 17 / installer 5（含 0.1.7 受控目标真实走通）/ invariants 15 / local-client 3 / maintenance 6 / snapshot 5 / candidate 4 全 OK，session-recovery node 1 pass + 7 skip（需真实树）。P1 的 freeze 3 fail / release 1 error 确认为非 root 环境性。
- **证据（csai `20260926-017/`）**：`p2-evidence/root-tests.txt`（sha256 `1c726e6d4c9b1e27bc8a84df93cb6062df86e5b692717d8bda091f6faee690b1`）、`local-tests.txt`（`11b773a0…`）、`tools-sha256.txt`（`4bf83048…`）、`changed-files.txt`（`5e37aabf…`）、`scheduler-digest.txt`（`6db472dd…`）、`prod-invariant.txt`（`ea05b3be…`）；`p2-tools/` 工具副本逐文件 SHA 与仓库一致。
- **生产零影响核验**：silksecagent active / MainPID 635167 / NRestarts=0；`app/package.json`（`c49b1157…`）、`dsh/package.json`（`0d9ea2f7…`）、`plugins/sec-domain-task/index.js`（`6b21fb7e…`）SHA 不变；窗口 journal 无 err。
- **记录已提交**：record §5.1–5.4 更新 + §9「P2 执行记录」；state gates 更新；PROGRESS 一行；commit `feat(secagent): DSH 0.1.7 升级链 P2——P0-1 版本解放与工具链修复`。

# P3 本步任务（U1b · P0-2 Session V4 + P0-3 Agent Preset）

> 目标：把升级工具链的 Session 断言/发布语义从 V3 升级到 **V4**，并按 0.1.7 的 `@deepseek-ai/dsh-agent-preset` bundle patch 机制替换 `.agent-presets/` 目录注入。**本步不改生产、不重启、不运行 setup、不部署 bundle**；全部改动在 `bundles/dsh/templates/` 内完成并本地/远程隔离测试。

1. **前置校验**：核对 STATE 与实际（`git log` HEAD 含 P2 提交、`upgrade_dir` 的 precheck 4 份 + p2-evidence 6 份 SHA、`spool service csai status` 生产健康、P2 工具 SHA 与仓库一致）；不一致按硬停止处理。
2. **P0-2 Session V4 适配**（依据 plan §P0-2 与 25 号方案）：
   - 上游资源：`/tmp/opencode/upstream-017/dsh-session-format-v3-to-v4-0.1.7-rc.2.tgz`（临时，若缺失按 P1 方式从 npm 重取并记录 integrity）。其 `package.json` 的 `dsh.sessionFormatMigration` 已声明：`from=3,to=4`，导出 `sessionFormatV3ToV4`、`releasedV3SessionFormatCodec`、`releasedV4SessionFormatCodec`、`assertReleasedV4Header`、`restoreReleasedV4Artifact`。先读其 `lib/index.js` 与 `lib/types/migration.d.ts`，按真实 API 写断言，**不要猜 API**。
   - `dsh-session-rehearsal.mjs`：`create({ version: 3, ... })`（:131）与 `session.v3.jsonl` 断言（:196）、`target_version: 3`（:247）→ V4；迁移器调用改为上游 v3-to-v4 通道或按 target codec 校验（保持「原件不变、副本迁移、失败显式」语义）。
   - `dsh-session-recover.mjs:128` 发布文件名 `session.v3.jsonl[.zstd]` → V4；V0 冲突恢复分支（legacy 0.1.2 codec + target strict restore）P3 必须显式验证目标 0.1.7 下的语义：若 strict `validation:'current'` 不接受 V0，优先按上游迁移链「V0→（旧）V3→V4」组织或明确保留该分支仅在 0.1.5 目标下可用并 fail-closed 拒绝 0.1.7，**不得丢失既有拒绝/对账语义，不得静默降级**；决策写入 record。
   - `dsh-upgrade-release.py:264` `rglob("session.v3.jsonl*")` 与 :256-266 的 V3 拒绝覆盖语义 → V4；`recover_legacy`/`rehearsal` 调用链的参数与报告字段同步。
   - `dsh-upgrade-session-read.mjs`：V4 读句柄/事件读回路径适配；`dsh-upgrade-invariants.py`：若存在版本敏感断言则适配（P2 已 grep 未见 V3 字面量，仍须逐项确认）。
   - `dsh-upgrade-release.py` 的 `VERSION`/`OLD_VERSION` 已参数化，不要回退；`0.1.5` 路径行为保持逐字不变（其分支/默认值保留）。
3. **P0-3 Agent Preset 机制替换**（依据 plan §P0-3）：
   - 上游资源：`/tmp/opencode/upstream-017/dsh-agent-preset-0.1.7-rc.2.tgz`（peer：`dsh-agent-preset-registry@0.1.7-rc.2`）与 `dsh-web-app` 依赖；读 `lib/index.js` 与 types，确认 preset 行 schema（bundle patch 行的 id/config 形态）与 registry 行为。
   - 7 角色（recon/vuln-hunt/biz-logic/code-audit/intranet/review/orchestrator）从「写 `data/.agent-presets/<id>/agent.cordis.yml` 目录」改为「由部署链落 bundle patch preset 行」；`seed-presets.sh`、`dsh-plugin-sec-suite.persona.py`、`dsh-plugin-sec-suite.host-compat.js`、`dsh-upgrade-runtime-smoke.py --personas` 按新机制改造；旧 `.agent-presets/` 保留**只读备份**并记录 SHA。
   - 保留授权/工具指导/运行上下文约束：角色 prompt 不得为空、不得回退 legacy `text`、工具行（sec-suite/asset-graph/experience/proxy-pool/sec-domain-bus）与 `sec-domain-bus` sidecars=false 不变；`persona_version=6` 语义由 P3 给出新定义（例如 `preset_version`），并在 record 记录迁移口径。
   - 单测/契约：`dsh-plugin-sec-suite.persona.test.py` 的 0.1.7 用例由「显式拒绝」改为真实走通（P2 留的 `test_future_version_layout_is_not_silently_treated_as_legacy` 须被替换）；补 7 角色最终 prompt/工具集合断言。
4. **测试（本步验收核心）**：本地 9 组 Python + node；csai root 复跑（工具经 base64/tar 传入 `upgrade_dir` 新子目录，如 `p3-tools/`，**不得覆盖 `/opt/silkspool/dsh/`、不得重启**）；node fixture 需覆盖 0.1.7 目标版本；`dsh-upgrade-rpc.test.mjs` 仍无候选（P5 才可跑），如实记录不填「通过」。
5. **记录与提交**：
   - record 追加「P3 执行记录」（改动清单、V3→V4 与 preset 机制旧→新对照、测试结果/退出码、证据 SHA、决策记录含 V0 分支处置与 preset 版本口径）。
   - state：`gates.session_v4: pass`、`gates.agent_preset: pass`（按实测如实）、open_issues 更新、`last_commit`。
   - PROGRESS 一行更新；`git add` 指定文件（禁用 `-f`/`-A`）；commit `feat(secagent): DSH 0.1.7 升级链 P3——Session V4 适配与 Agent Preset 机制替换`；push 当前分支。
6. **输出**：① 执行摘要与证据；② 风险/未决；③ `===== NEXT PROMPT (P4) =====`（含更新 STATE；P4 任务：P0-4 settings.yaml→Profile 插件配置 + P0-5 第三方插件升版与计费补丁重写 + P0-6 部署链「先 dump-config 后重启/部分激活检测」+ runtime-compat 0.1.7 分支落地 + 相关单测/契约 + 提交），并写入 `handoff-017/P4.md`。

# 验收门禁（P3）

- **V4 语义用真 API**：断言依据上游 `dsh-session-format-v3-to-v4` 实际导出写就，`session.v4.jsonl` 发布与读回在 fixture 级走通；不得只替换版本字符串。
- **向后兼容**：0.1.5 路径行为逐字不变；既有拒绝语义（seq gap/引用/父子继承）全部保留。
- **Preset 机制**：7 角色在新机制下生成成功且有最终 prompt/工具集合断言；旧 `.agent-presets/` 只读备份可追溯。
- **零生产影响**：不重启、不 setup、不写 `/opt/silkspool/dsh/`、不改运行态 freeze 配置。
- **记录纪律**：所有「通过」都有 时间+命令+退出码+输出/SHA 支撑；未跑/未验证项显式列出。

# 硬停止输出格式（如触发）

```
===== CHAIN BLOCKED =====
失败步：P3
事实：<时间/命令/退出码/关键输出>
影响：<生产是否受影响>
恢复建议：<具体回滚/修复路径>
```
