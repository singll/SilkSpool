# ===== NEXT PROMPT (P4) =====

你是 SilkSecAgent v5 的 DSH 升级执行 agent。本会话执行「0.1.7 升级链」第 4/8 步：**P4 · U1c settings.yaml→Profile + 第三方插件升版/计费补丁重写 + 部署链「先 dump-config 后重启」**。

## 恒定段（每一步都必须随 NEXT PROMPT 原样携带）

### 授权（用户已一次性授予，严禁逐步确认）
允许无需回问直接执行：仓库读写/本地测试/git commit+push；`spool exec/query/dispatch`；`rsync -a bundles/dsh/ /opt/SilkSpool/bundles/dsh/`；`spool bundle dsh setup csai`；`spool restart csai silksecagent`；在 `/opt/silkspool/dsh-upgrades/` 下创建/清理升级工作目录；冻结/切换/回滚工具（P5 起）；`bus.prune` 维护；观察期重启与巡检。
**U3 生产切换预授权**：P5 全部门禁为绿即自动执行 P6，不再回问；失败立即回滚并按硬停止输出。
**红线**：一切远程操作走 PATH 中的 `spool`（禁止直连 SSH/curl 操作远程 Docker）；禁止 `docker compose down`（尤其 n8n 禁 `-v`）；禁止 `git add -f`；不提交 keys/config.ini 等被忽略文件；改 bundle 模板后必须先 rsync 运行时副本再 `spool bundle dsh setup csai`；Session 迁移必须先有完整冻结点 + 恢复预演。
**硬停止条件（仅这 4 类，遇到即停下并输出 CHAIN BLOCKED）**：① P5 任一门禁失败且当前步无法修复；② 生产不变量出现白名单外的非预期差异；③ 需要超出预期的停机且无法自动恢复；④ 用户喊停。其余一律自行决策继续。

### 基线（已定，不要重新论证）
- 目标版本：DSH **0.1.7-rc.2**（GitHub commit `477b4f420553`，release 2026-09-24T14:10:21Z；npm dist-tag `next`）。若期间上游出现更新 rc，只在记录中注明，**不切换目标**（除非用户明确指示）。截至 P3 无 0.1.7-rc.3。
- 现役生产：0.1.5-rc.2，csai `/opt/silkspool/dsh/`，web 服务 `silksecagent`（edge `silksecagent-edge`）。
- 已定决策：官方 `dsh-schedule` 不接入；dsh-bill 升版并重写计费补丁；保留窗口「7 天或 2 万行取大」；自研 interval 下限保持 300s；受管 preset 只写 web profile patch（headless 拒绝带 preset 的 Session）；V0 冲突恢复 0.1.7 fail-closed（生产 0 个 V0-only 会话）。
- 执行真相源：`doc/secagent/25-dsh-0.1.7-upgrade-and-scale-2026-09-26.md`（§一 P0-1..P0-6 与 U0–U4 为准）。
- 流程/格式范例：`doc/secagent/archive/upgrades/TEMPLATE.md`、`2026-09-12-dsh-0.1.5-rc.2-plan.md`、`-record.md`、`HANDOFF-u3-switch.md`。
- 升级工具位于 `bundles/dsh/templates/`（setup.sh / dsh-upgrade*.py|sh / dsh-session-*.mjs / dsh-version-watch.sh / dsh-runtime-compat.py）。

### 步骤地图（恒定，用于生成下一步）
- P1 U0 预检与建账 → P2 U1a 版本解放与工具链修复 → P3 U1b Session V4 + Agent Preset → **P4 U1c settings + 第三方插件 + 部署链** → P5 U2 隔离预演（全绿自动放行）→ P6 U3 生产切换 → P7 U4a 巡检 1 → P8 U4b 巡检 2 + 关账（CHAIN END）。

### STATE 模板
```yaml
current_step: P4
target_version: 0.1.7-rc.2
target_commit: 477b4f420553
current_prod_version: 0.1.5-rc.2
upgrade_dir: /opt/silkspool/dsh-upgrades/20260926-017
candidate_dir: ''
freeze_state: ''
release_dir: ''
observation_until: ''
gates: {}
open_issues: []
last_commit: ''
```

### NEXT PROMPT 递归规则（每步结束必须执行）
1. 每步结束输出恰好一个 `===== NEXT PROMPT (P<n>) =====` 围栏代码块；内容自包含（新会话零上下文可用）：角色、恒定段、更新后的 STATE、上一步完成事实（时间/产物/SHA/证据）、本步任务、验收门禁、记录要求、本递归规则。
2. 同步把该 prompt 写入 `doc/secagent/archive/upgrades/handoff-017/P<n>.md`。
3. P8 关账成功输出 `===== CHAIN END =====`；任一硬停止输出 `===== CHAIN BLOCKED =====` + 失败事实 + 恢复建议（仍属终止）。
4. 每步开头先校验 STATE 与前置产物（文件/SHA/服务状态），不一致按硬停止处理。
5. 记录纪律：证据 = 时间戳 + 命令 + 退出码 + 关键输出 + SHA-256；不填未验证的「通过」，失败据实记录并给归因。

## 本会话 STEP STATE（P4 开始前，来自 P3）

```yaml
current_step: P4
target_version: 0.1.7-rc.2
target_commit: 477b4f420553
current_prod_version: 0.1.5-rc.2
upgrade_dir: /opt/silkspool/dsh-upgrades/20260926-017
candidate_dir: ''
freeze_state: ''
release_dir: ''
observation_until: ''
gates:
  toolchain_version_liberation: pass
  session_v4: pass
  agent_preset: pass
open_issues:
  - P4 必做：settings.yaml `agent-presets.default: vuln-hunt` 迁移到 web patch 的 agent-preset-registry 行 config.default，否则默认角色静默回落 standard；同文件有 preset 受管块（BEGIN/END），写入不得破坏
  - freeze 工具默认模块 plugins/sec-domain-task/index.js 且 scheduler_module 可注入；运行态 freeze-config-current.json（20260913-rc2/）未动 → U2 冻结前用 scheduler-digest 生成新配置（P5）
  - dsh-runtime-compat.py 0.1.7 分支显式 raise PENDING_017：patch_settings/patch_theme/patch_billing 按新运行产物重写属 P4
  - P5 必读性能：0.1.7 每会话迁移全库扫 zstd 首行（上游 listArtifacts）→ O(N²)，csai 实测 ~2-4s/会话（1500+ 会话约 1-2h）；release.py rehearsal 超时已 1800→14400s；U3 冻结窗口方案需 U2 决策（接受窗口 / 按 manifest 复用 U2 预演产物）；禁止按项目切分来源（20 子会话跨项目）
  - dsh-upgrade-session-trace.mjs 仍用 header.seedLength 读账单投影（0.1.7 为 inheritedEventCount；dsh-bill 0.18.1 投影 API 变化）→ P4 计费补丁重写时一起改
  - 独立调用 release/maintenance/egress/session 工具须显式携带 DSH_TARGET_VERSION=0.1.7-rc.2 + DSH_OLD_VERSION=0.1.5-rc.2（dsh-upgrade.sh 自动导出）
  - 官方 schedule 保持不接入（决策，非缺陷）
last_commit: <P3 提交 SHA，见 record §10>
```

## P3 已完成事实（2026-09-26，北京时间 ~22:10–24:50 / UTC 14:10–16:50）

- **Session V4 适配（15 个文件改动，全在 `bundles/dsh/templates/`）**：
  - `dsh-session-rehearsal.mjs`：目标代次改用 `sessionFormatCatalog.currentVersion`（真 API，0.1.7=4）；create/读回/发布/锁 fixture 全按目标代次；新增 `inheritedEventCount` 断言；`session.v4.jsonl` 发布后独立读回。
  - `dsh-session-recovery.mjs`：0.1.7 目标 **fail-closed**（V4 目标 catalog 显式拒绝 V0 冲突恢复；0.1.5→V3 路径逐字保留）；`dsh-session-recover.mjs` 发布代次参数化。
  - `dsh-upgrade-release.py`：`target_publish_glob()`（0.1.7→`session.v4.jsonl*`）；0.1.7 恢复报告可省略（0.1.5 仍必填）；跨代报告显式拒；`validate_managed_presets()` 封存校验；rehearsal 超时 14400s。
  - `dsh-upgrade-session-read.mjs` 增 `target_version` 报告；`dsh-upgrade-invariants.py` 确认无版本敏感断言（未改）。
- **Agent Preset 机制替换**：7 角色从 `.agent-presets/<id>/agent.cordis.yml` 目录改为 **web profile patch（`$DSH_HOME/profiles/web/cordis.patch.yml`）的 `@deepseek-ai/dsh-agent-preset` 受管块**（`# silksec-managed-agent-presets BEGIN/END`，`insert` 行，`config.plugins` = shipped standard plugins + persona + 5 工具行，order 10–16）；`preset_version=7`（persona_version=6 的新口径）；`persona.py seed/read-preset`、`host-compat.createPersonaReader`、`dsh-upgrade-runtime-smoke.py --personas`、`data-seed/scripts/discipline-audit.py` 全部改新读法（audit 按安装版本兼容旧目录，回滚可审计）；`dsh-upgrade-candidate.py` 0.1.5 才携带旧目录；生产旧 `.agent-presets/` 14 文件 manifest `9fbfad0f…` 保留只读备份。
- **测试（全绿）**：本地 9 组 Python（freeze/release 非 root 环境性失败同 P2）+ node host-compat 9/9；**csai root p3-tools（final 工具，零生产写入）9 组 Python 全 OK + node 9/9 + session-recovery 2 pass/8 skip**（`p3-evidence/root-tests.txt` `61974a7d…`）；沙箱真树：V3/V0→V4（`session.v4.jsonl`、原件不变、4 项写锁）、0.1.5 全 app 演练仍发布 V3、真实 preset schema（`AgentPreset.Config`）7 行×24 plugins 通过；0.1.7 真树 fail-closed 断言通过。
- **V4 事实依据**：V3 读→逻辑 v4、写→`session.v4.jsonl`、plain catalog 对 V0/V3 恢复抛「requires explicit historical child facts」、V0-only 可 V0→V4（exp1.mjs，真包+生产真实样本）。
- **全量生产副本预演**：0.1.7 最小真树（`p3-app017`）+ 生产 sessions 只读副本跑 `dsh-session-rehearsal.mjs`，见 record §10.6（结果/SHA 以 record 为准）。
- **生产零影响**：silksecagent active / MainPID 635167 / NRestarts=0；`app/package.json c49b1157…`、`app/node_modules/@deepseek-ai/dsh/package.json 0d9ea2f7…`、`plugins/sec-domain-task/index.js 6b21fb7e…` SHA 不变；journal 窗口无 err。
- **记录**：record §10（含 §10.7 性能发现、§10.8 零影响、§10.9 证据表）；state gates `session_v4/agent_preset = pass`；PROGRESS 一行；commit `feat(secagent): DSH 0.1.7 升级链 P3——Session V4 适配与 Agent Preset 机制替换`。

## P4 本步任务（U1c · P0-4 settings + P0-5 插件 + P0-6 部署链）

目标：把 0.1.7 运行所需的 settings 落点、三个社区插件与部署链校验补齐，让 **U2 可产出并启动候选**。本步不改生产、不重启、不运行 `dsh-runtime-compat.py` 于生产、不部署 bundle；全部改动在 `bundles/dsh/templates/`（含 `data-seed/scripts/`）内完成并本地/远程隔离测试。

1. **前置校验**：STATE 与实际（git log HEAD 含 P3 提交、`20260926-017/p3-evidence` 5 份 SHA、`p3-tools` 15 文件 SHA、生产健康、`p3-app017` 存在）；不一致按硬停止。
2. **P0-4 settings.yaml→Profile 插件配置（0.1.7）**：
   - 上游事实先读：`@deepseek-ai/dsh-settings@0.1.7-rc.2` 的 README/lib/types（导入一次性、`.imported` 改名、跨 profile 不共享、落点 `data/profiles/<p>/cordis.patch.yml` 或等价 patch）；**不要猜 API**。
   - 模型/provider/默认模型/retryPolicy 与 `agent-presets.default: vuln-hunt` 显式写入 **web 与 headless 双 profile** 的落点（`--dump-config` 断言关键 row：`agent-default-model`、`agent-preset-registry.config.default=vuln-hunt`、`llm-*` 等）；`setup.sh` 不再把 `settings.yaml` 当热更新真相源；保留 preset 受管块标记不被破坏（复用/扩展 persona 的受管块机制或 append 到块外）。
   - 删 `dsh-runtime-compat.py::patch_settings` 与 `patch_theme`（先评估上游主题修复；0.1.5 路径逐字保留）；`settings-mirror-patch.sh` 按新落点改造。
   - `data-seed/scripts/data-quality.py`、`dsh-upgrade-egress.py`、`dsh-upgrade-runtime-smoke.py`/`dsh-upgrade-worker-smoke.py` 改读**生效配置**（`--dump-config` 或 patch 文件），不再解析 `settings.yaml`。
   - `dsh-upgrade-invariants.py` 受保护路径：`data/settings.yaml` → 生效配置路径（首启后 `.imported`）。
   - 模型页回归（B6）：UI 编辑后是否持久化到 profile patch（U2 实机验证；P4 给出断言/检查方案）。
3. **P0-5 第三方插件升版 + 计费补丁重写**：
   - `dsh-auth-gate` 0.7.2→0.15.0、`dsh-model-failover` 0.1.4→0.1.5、`dsh-bill` 0.13.1→0.18.1；`plugins.lock` 与各 setup 脚本 pin 同步（受控版本集合/未知拒绝沿用 P2 风格）。
   - **重写 `dsh-runtime-compat.py::patch_billing*`**（0.1.7 分支，替换 `PENDING_017`）：按 dsh-bill 0.18.1 真实投影 API + Session V4 `inheritedEventCount` 语义；同步改 `dsh-upgrade-session-trace.mjs`（`header.seedLength`→ 新语义）；成本归因链路（26 号）不得断；billing row id `plugin-package-inventory-deepseek`/`session-telemetry-otel`/`session-log-deepseek` 保留。
   - 浏览器 fork 重跑 browser-scope/browser-smoke 验收；`dsh-sentinel` 维持移除。
   - 登录门/fallback 链回归：U2 实机（P5），P4 至少给出 fixture/契约。
4. **P0-6 部署链**：
   - 所有 setup/升级写配置后**先 `--dump-config`（必要时 `--dump-config-schema`）通过再重启**；升级脚本识别「部分激活」并给重试/回滚路径。
   - 自有插件不声明 `peerDependencies`；被拦则 exact-version 例外并记录。
   - `dsh-upgrade-candidate.py` 0.1.7 全链走通（P2 起的 `PENDING_017` 必须消失，候选能构建到 `prepared`）。
5. **测试（本步验收核心）**：本地 9 组 Python + node；csai root 复跑（工具经 base64/tar 传入 `p4-tools/`，不得覆盖 `/opt/silkspool/dsh/`、不得重启）；0.1.7 fixture 覆盖 settings 落点/插件 pin/billing 补丁/dump-config 断言；`dsh-upgrade-rpc.test.mjs` 仍无候选（P5），如实记录不填「通过」。
6. **记录与提交**：record 追加「P4 执行记录」（改动清单、settings/patch 落点旧→新、插件版本与补丁重写对照、测试结果/退出码、证据 SHA、决策含默认 preset/主题补丁去留）；state gates `settings_migration/third_party_plugins/deploy_chain`（按实测如实）、open_issues 更新、last_commit；PROGRESS 一行；commit `feat(secagent): DSH 0.1.7 升级链 P4——settings/第三方插件/部署链适配`；push 当前分支。

### 验收门禁（P4）
- `dsh-runtime-compat.py` 0.1.7 分支可跑通并产出确定性补丁（0.1.5 路径逐字不变）；候选构建链（candidate→setup→seed→runtime-compat）能在隔离环境走到 `prepared`。
- settings 双 profile 落 patch 且 `--dump-config` 关键 row 断言通过；`agent-presets.default=vuln-hunt` 不回落；删除了 patch_settings/patch_theme 的 0.1.7 依赖。
- 插件 pin/锁一致；billing 补丁按 0.18.1 真实 API；`dsh-upgrade-session-trace.mjs` 与 V4 语义一致。
- 零生产影响：不重启、不 setup、不写 `/opt/silkspool/dsh/`、不改运行态 freeze 配置。
- 记录纪律：所有「通过」都有 时间+命令+退出码+输出/SHA 支撑；未跑/未验证项显式列出。

### 硬停止输出格式（如触发）
```
===== CHAIN BLOCKED =====
失败步：P4
事实：<时间/命令/退出码/关键输出>
影响：<生产是否受影响>
恢复建议：<具体回滚/修复路径>
```

### 本步结束时输出
① 执行摘要与证据；② 风险/未决；③ `===== NEXT PROMPT (P5) =====`（含更新 STATE；P5 任务：U2 隔离预演——冻结/候选/释放 prepare/全量 Session V4 迁移+preset/settings/插件实机验收/最小维护/不变量 failures=0/回滚/旧版读回 100%，并显式决策全量迁移冻结窗口方案），并写入 `handoff-017/P5.md`。
