# DSH 0.1.5-rc.2 升级记录：csai 预检与首次实施

> 检查日期：2026-09-12；运行态聚合输出保存于 20:40:44～21:00:37（北京时间，12:40:44～13:00:37 UTC；依据本地证据文件 mtime，不作为命令起止时间）
> 当前状态：**2026-09-15 完成 U2 整套重验（新副本 asrhrri9）后，经用户批准进入 U3 生产切换：以当日新鲜冻结点 `dsh-snapshot-ready-8jhp928l` 走完 prepare→switch→rc.2 最小维护验收→生产不变量门禁→finalize 恢复写者，生产现为 0.1.5-rc.2、MainPID 3848865、NRestarts=0、active/running，进入 72 小时观察期（至 2026-09-18T14:16:35Z）。U4 观察期未满，不得宣告升级关账；§14.5 已记录观察期内首次巡检（MainPID/NRestarts/journal/调度器/vault/别名闸口全部符合预期，兼容别名观察期因33条切流前 deprecated_use 顺延至 2026-09-22T15:07:10+08:00）；L0–L6 未执行**。§1–8 为 9 月 12 日基线，首次实施见 §9，续作见 §10–13，生产切换见 §14，观察期巡检见 §14.5。
> 配套：[升级方案](2026-09-12-dsh-0.1.5-rc.2-plan.md) · [自学习设计](2026-09-12-self-learning-design.md) · [时间线](README.md)
> 代码基线：检查开始时 HEAD `d9133f8`；远程操作全部经 PATH 中的 `spool exec csai`。

## 1. 检查方法和可信度

- **运行态实测**：systemd 元数据、只读 SQLite（URI `mode=ro` + `PRAGMA query_only=ON`）、文件元数据、脱敏聚合、既有 `sec-v5-accept.sh --json`。
- **源码核对**：v5/00–18 的相关契约与最新审查、PROGRESS、旧升级记录；本地模板与线上插件/脚本抽样 **15/15 SHA-256 一致**（模板只替换 BASE_DIR 后比较）。
- **上游核对**：GitHub 两个 release 页面、rc.2 标签源码、npm 两个版本端点；GitHub API 因限流返回 403，改读公开发布页及标签源码，未将 API 失败当成无发布。
- **没有执行**：升级、setup、重启、评测集写入、生产漏洞扫描、昂贵模型评测、BrowserAuth 登录和完整浏览器 E2E。旧记录中的“347/347 契约通过”属于之前会话的结果，不冒充本次重跑。

原始聚合输出保存在管理机临时目录 `/tmp/silksecagent-upgrade-20260912-8p6gba1b/`；可长期使用的结论已写入本文件，不依赖临时目录继续存在。

## 2. 平台与升级基线

| 项目 | 实测结果 |
|---|---|
| DSH 应用依赖与已安装包 | 均为 **0.1.2-rc.1** |
| 服务 | `silksecagent` active/running；MainPID 3683937；NRestarts=0 |
| 本次启动时间 | 2026-09-12T15:23:34+08:00 |
| 基础验收 | **PASS=25，FAIL=0**；6 个服务、data-quality、授权声明、事件目录、双 profile 组合树及 14 业务域文件 |
| 领域数量 | **14 业务域 + bus = 15 个域插件**；scope 是唯一授权域名 |
| Node / pnpm | v22.23.2 / 11.22.0 |
| 社区插件 | web：auth-gate 0.7.2、model-failover 0.1.4、dsh-bill 0.13.1、browser fork 0.1.0-silksec.1；headless 安装 failover 0.1.4 |
| 生产主目录 | `/opt/silkspool/dsh/`；应用在 app/，主要状态在 data/ |
| 会话文件 | data/sessions 下 249 个压缩日志；根 sessions 下另有 2 个，均为 .jsonl.zstd；**仅点数，未解码验证日志格式** |
| 代理变量 | 根 .env 存在 SEC_EGRESS_PROXY / SEC_FLOW_PROXY；该文件未声明 HTTP_PROXY / HTTPS_PROXY / ALL_PROXY / NO_PROXY。未读取或输出凭据值，也未据此断言进程没有其他来源的环境变量 |
| 生产配置改动 | 无 |

**25 项通过的含义有限**：dump-config 能证明配置包含插件，不能证明每个 handler、看板登录或学习评测链路都运行正确。下方确定性缺口正说明升级需要增加功能验收。

## 3. 数据、调度和事件

| 项目 | 实测结果 | 解读 |
|---|---|---|
| assets / endpoints | 96,690 / 145 | 资产规模大，接口级结构化覆盖需单独度量，不能把资产总数视为有效探测覆盖 |
| findings | 82；noise=0 共 46；noise=1 且 new 共 0；noise=1 终态共 36 | 候选池口径正确；noise=0 中 confirmed=38、new=2、dup=5、false_positive=1 |
| facts / fact_edges | 806 / 761 | 事实有沉淀；facts.uses 总和为 0，需检查使用回执，不能推断从未检索 |
| tasks / task_runs / workers | 37 / 147 / 183 | worker 终态：done 143、failed 24、killed 16；检查时无 running worker |
| 活跃 interval | #24、#37、#100007、#100008 均 queued | 历史 done/cancelled 的 interval 行不能算活跃调度 |
| 调度持有者 | scheduler.lock 的 pid=3683937，与 web 主进程相同 | 源码仍由 v4 scheduler 持锁；v5 调度替换尚未收口 |
| 近三天 #100007 | ok=0 共 10 次、ok=1 共 3 次，最新已成功 | 历史故障需分类，不能都算“漏洞探测阴性” |
| event_outbox | 3,178 条全 delivered，max_retry=0 | 当前积压/死信为 0；不代表下游 partial 结果都已成功处理 |
| bus_subscription | 99 条全 delivered，max_attempt=0 | 同上 |
| approval_effects | 3 条 applied | 当前无待应用 effect |
| SQLite | WAL | 没有本次改为单写者守护进程的依据 |
| 新进程 journal 定向检查 | SQLITE_BUSY / database is locked / E_BUS_DOMAIN_UNKNOWN / already registered 等选定模式计数均 0 | 仅该启动窗口和模式范围内的结果 |

### 别名观察期

累计 `deprecated_use=147`，包含 run_cli、worker_status、coverage_report 等，**不只评测 fixture 在使用**。最后一条是 **2026-09-12T15:23:24.420+08:00**。

连续 7 天零使用的最早闸口因此变为 **2026-09-19T15:23:24.420+08:00**；有新调用继续顺延。不能沿用 PROGRESS 之前的"约 9 月 18 日"，更不能现在删除 37 个兼容别名。

**U4 巡检更新（2026-09-15T14:57Z）**：`audit.jsonl` 累计 `deprecated_use=180`，旧闸口（2026-09-12T07:23:24Z）后又新增 **33** 条，最后一条 **2026-09-15T07:07:10.543Z**（工具 `grep_result`，切流前产生）。切流后（≥14:16:33Z）`deprecated_use` 增量为 **0**。新最早闸口顺延至 **2026-09-22T15:07:10.543+08:00**；若 L0–L6 评测或其他调用再次触发别名，闸口继续顺延。37 个别名在任何情况下不得在闸口前删除。

## 4. 自学习实际基线

| 项目 | 实测结果 | 解释 |
|---|---|---|
| exp_cards | 39；active 35、candidate 2、cooling 1、deprecated 1 | kind=playbook 共 31（其中 active 29）；旧 playbooks 表为 0 是迁并结果，不是没有剧本 |
| 经验卡计数器 | uses 634、adopted 383、pos_fb 26、neg_fb 0；runs 421、successes 401 | 混有历史迁移/模型回执/执行结果，**不能据此声称漏洞检出成功率为 95%** |
| exp_feedback 原始流水 | 11：adopted 8、useful 2、validated 1；wrong/outdated 0 | 负反馈与计数器来源对账是学习闭环的优先项 |
| 经验向量 | 总 10 行；active 卡实际覆盖 **6/35** | 不能按“已安装 embedding”认定语义检索完整；需可恢复的补建与覆盖监测 |
| kb_docs | 391：active 312、curated 79；60 篇 uses>0 | 有文献使用，但需区分检索曝光与实际采用 |
| 文献向量 | active 覆盖 **225/312**；curated 0/79 | curated 的 FTS 路径可独立使用，向量指标应分组计算 |
| rules / vulncards | 79 篇 Markdown 规则 / 18 个 YAML 漏洞卡 | 不能把 registry.md 计成唯一漏洞卡 |
| 凭据引用 | 1 条 | 仅统计引用，未读凭据；不能据此认定已有跨账号/跨租户验证所需的成对测试身份 |
| 活评测集 | 44 行：confirmed 28、false_positive 16；**33 个不同 finding_id** | 先按 finding 最新裁决重算，再区分裁决来源；44 行不是 44 个独立样本 |
| FP / 契约种子 | 12 / 7 用例 | 覆盖面有限 |
| 最近契约报告 | 2026-09-12T01:42:30.868+08:00；gateway **7/7** | 既有报告读取；只证明这些网关用例通过 |
| 当前 FP / range 标准报告 | fp-report.json 与 eval-range-report.json 均缺失 | 不是“从未评测”；下面另有历史范围报告 |
| 历史靶场报告 | 4 份，2026-08-21T01:35～12:11+08:00；分别 3/6、1/3、4/6、1/2 | 不同批次不直接比较，不能作为 v5 + rc.2 的检出基线 |

## 5. 本次确认的适配点和缺口

| 编号 | 结论与证据 | 进入哪个增量 |
|---|---|---|
| C1 | 新版 sessionPersistence.list() 返回 snapshot.header；现有 sessionsList、reconcileWorkspaceSessions、findWorkerSessionId 按平铺 header 读取，需统一适配 | U1 |
| C2 | 新版 persona 用 prefix/suffix，standard 归 dsh-agent-presets；seed-presets 的旧包路径和 text 正则、scheduler 的 text 提取均需修改 | U1 |
| C3 | 原升级脚本在线 tar，备份失败只 warn；配置包备 package-lock 却漏 pnpm-lock/workspace/patch 集；回滚 pnpm install 在 CI 下还可能触发 frozen-lockfile | U1 |
| C4 | 新版 Web 的 sidebar.footer.action **仍存在**；不把它误报为必坏。新 main/panellist 可用于后续看板改造 | U2 验证、L6 优化 |
| C5 | 自研模板没有直接 ctx.agent / agentLoop.create / Inbox 构造依赖；exec.agent 是仍存在的 ToolRunContext，child.pid 来自 node:child_process，不能机械删除 | U2；社区插件另验 |
| K1 | **kb 分类与失败计数缺列**：线上 kb_docs 没有 category / fetch_failures；只读 EXPLAIN 两条相应语句分别报 no such column。对应源码 kb_list(category)、kb_revalidate(fetch_failed) 会走这些列 | L0 |
| K2 | kb_revalidate(changed) 目前只更新验证时间；没有落实正文版本、内容差异与索引更新，不能当成情报已刷新 | L0/L2 |
| K3 | eval-run.js 直接注册旧 sec-suite 并找 run_cli/grep_result；这些注册已移出，未通过 v5 总线。另写 report-时间戳.json，与标准 range 摘要入口脱节 | L0/L3 |
| K4 | eval 的 runContract(llm_probe=true) 仍仅 dispatchAttempt；参数只改变用例选择和 mode 标签，**没有真正的模型诱导调用** | L0/L3 |
| K5 | 当前 scheduler 仍组装含 finding_add 的提示；discipline-audit 的“悬空引用为 0”不代表运行时拼装字符串无旧引用 | U1、5.2 的观察前置 |
| K6 | 证据跨沙箱不可见、dashboard 的 v4 fallback、task 守卫异常被当成空缺失列表，以及订阅 partial 吞错 | v5 最新逐域审查已记录；U1/L0/L6 按影响修复 |

K1 的 EXPLAIN 只编译语句，没有执行 UPDATE。K2–K5 是与线上一致的源码核对，没有对生产执行写入复现。K6 的既有事件背景见 [vuln](../v5/02-vuln.md)、[task](../v5/05-task.md)、[dashboard](../v5/16-dashboard.md) 文末最新审查；本次没有重跑原故障目标。

## 6. 上游版本核实

| 来源 | 结果 |
|---|---|
| [GitHub releases](https://github.com/deepseek-ai/deepseek-harness/releases) | 页面最新顺序为 rc.2、rc.1 |
| [rc.1](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.5-rc.1) | 发布时间 2026-09-10T03:09:00Z；tag commit 183f08e9c6dde7e36cd2318eaee70b0da08fb35e |
| [rc.2](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.5-rc.2) | 发布时间 2026-09-10T15:09:34Z；tag commit fb2c4b9e698e30edb738bca4cf0618587db7d203 |
| [npm 指定 rc.2](https://registry.npmjs.org/@deepseek-ai/dsh/0.1.5-rc.2) | 可获取 0.1.5-rc.2 元数据；tarball shasum 2c78db39568d910868f1e4f34062a4f346d4815d |
| [npm latest](https://registry.npmjs.org/@deepseek-ai/dsh/latest) | 检查时仍为 **0.1.5-rc.1**；执行时需再次解析和记录 |

## 7. 执行流水与待完成项

| 阶段 | 状态 | 结果 |
|---|---|---|
| U0：文档、release、源码与 csai 预检 | 已完成 | 本记录；25/25 基础验收；15/15 抽样文件一致 |
| U1：升级脚本与接口适配 | 部分完成（2026-09-14） | Session/persona/worker/Web/browser 兼容、完整快照与隔离工具已有实现；冻结派单竞态、安全切换器及最终版本清单仍待完成 |
| U2：隔离迁移及回滚预演 | 已完成（2026-09-15，见 §13.4） | 新副本一次性跑完切换→rc.2 启动→不变量对照 v3→回滚→旧版启动→257/257 读回；双 profile、实际 Web/worker、浏览器 Scope、354 项契约、exp_fts 修复均有证据 |
| U3：生产切换 rc.2 | 已完成（2026-09-15，见 §14） | 新鲜冻结点 → prepare → switch → rc.2 最小维护验收 → 生产不变量门禁 → finalize；生产 0.1.5-rc.2、MainPID 3848865、active/running；14.1 修复两个工具缺陷；14.2–14.3 全部证据已记录 |
| U4：观察期与升级关账 | 进行中（2026-09-15 起至2026-09-18T14:16Z） | 72 小时生产观察；U4 到期后执行 preserve_after_resume（新冻结点 + 新增业务保全 + 对账报告），不以本次切换代替 ≥72h 生产验证。§14.5 记录观察期内首次巡检（2026-09-15T14:57Z）：MainPID 不变、NRestarts=0、journal 无崩溃、vault 回流 errors=0、调度器持锁存活、别名闸口因 33 条旧 deprecated_use 顺延至 2026-09-22T15:07:10+08:00 |
| L0–L6：学习与探测增量 | 未执行 | 详见专项设计 |

后续在本文件追加执行时间、版本锁、备份校验、提交与真实验收结果，再更新目录时间线。

## 8. 本次文档整理记录

- 建立 SilkSecAgent 总入口、升级时间线、通用记录模板、本次升级方案及自学习专项；三份历史升级文件从 archive/ 移入 upgrades/，保留历史实施结论。
- 更新 v5 导航、PROGRESS、会话模板及旧审查的时点说明；修正当前别名观察闸口与评测口径，历史里程碑不改写。
- 文档检查覆盖 23 个新增/修改后的 Markdown 文件：217 个本地路径链接、51 个标题锚点、11 个上游源码链接（对照 rc.2 标签检出目录）均通过；diff 空白检查通过。
- 本次变更仅为文档；没有改动或部署运行代码，未将现有基础健康检查扩称为完整功能验收。

## 9. 2026-09-13 执行记录

- 用户要求写全会话格式名称，并授权开始执行升级方案。文档已统一明确 Session V0 / Session V1 / Session V2 / Session V3；历史验收项 V1–V10 及缓存 schema 版本保留原义。
- 2026-09-13T10:45:57+08:00 复核：silksecagent active/running，PID 3683937，NRestarts=0；可用磁盘约 929G。生产仍运行原版本，尚未暂停、重启或切换。
- 先完成 U1 的兼容改动与升级工具加固，再按 U2 的隔离与恢复验收决定是否进入生产切换；实际结果随执行追加。

### 9.1 已实施的兼容切片

| 改动 | 实际行为与部署范围 |
|---|---|
| 版本术语 | 全文明确 **Session V0 / Session V1 / Session V2 / Session V3**，与 SilkSecAgent v5、DSH 包版本和缓存 schema 分开；历史验收项 V1–V10 保持原义 |
| Session 适配 | 三个宿主消费者共用 `host-compat.js`，兼容平铺 header 与 snapshot.header。无效形状、重复 ID、列表失败显式报告；列表不获取写锁 |
| worker 关联 | 时间窗内必须有可信时间戳且候选唯一；并发歧义不再取“最新一个”，后备关联有诊断。worker 自身 ID 的直接上报仍需在完整 worker 适配中接线 |
| persona 生成 | `persona_version=6`；从实际安装入口解析 shipped standard，保留旧 DSH 的 text 兼容。新版生成 prefix/suffix，保留 `complete:false` 与 `includeRuntimeContext:true`；先完整校验，再以 Linux `renameat2` 交换目录，旧目录留存 |
| persona 读取 | scheduler 与生成器共用 PyYAML 结构解析；`!!js` 只当作数据保留。缓存随文件指纹失效，未知变量/空角色/损坏配置导致派单失败，不缓存空人格 |
| 任务提示 | 移除运行时 `finding_add`，明确 `vuln_register_signal` 登记候选、`vuln_confirm` 经证据守卫确认；不把登记当作确认 |
| 提示审计 | 扫描 prefix/suffix、共享调度模板及 `--prompt-file` 输入；缺失/损坏角色显式告警；旧别名与悬空工具分别统计；SQLite 使用只读连接 |
| 安装接线 | manifest 与 suite 组装器包含兼容模块/解析器/测试；preset 失败不再只 warn。setup 补齐 python3-yaml/zstd 依赖，**DSH_VERSION 仍保持 0.1.2-rc.1** |
| Session 演练工具 | `dsh-session-rehearsal.mjs` 只在自动新建的私有副本上调用官方迁移器；逐会话保存结果、哈希、独立读回结果和写锁检查 |

模板已由仓库同步到 `/opt/SilkSpool/bundles/dsh/`，再通过 `spool exec csai` 暂存到独立候选目录。**没有把这些插件覆盖到生产 plugins/，没有执行全量 setup 或旧 dsh-upgrade.sh。**

### 9.2 候选与兼容验证

- 候选根：`/opt/silkspool/dsh-upgrades/20260913-rc2/candidate/`，与生产 `/opt/silkspool/dsh/` 分离；候选 app 显式安装 **0.1.5-rc.2**，采用 `pnpm install --prod --ignore-scripts --no-frozen-lockfile` 生成候选锁。此次没有启动候选 Web/headless 服务或调用模型。
- 再次核实 npm `latest` 仍为 **0.1.5-rc.1**；rc.2 tarball shasum 仍为 `2c78db39568d910868f1e4f34062a4f346d4815d`。
- 候选 `app/pnpm-lock.yaml` SHA-256：`74006f9a996d497d3184dece4a46c39fe8a2aa3c051cb8f0d79d9ccb57cf082a`。候选 app 检出 241 个不同的 `@deepseek-ai/*` 包，同名包未发现多个版本；这不覆盖尚未安装的 web/headless 社区插件依赖树。
- **17 项针对性测试通过**：Node 9 项 + Python 8 项，在管理机与 csai 的 Node 22.23.2/PyYAML 6.0.1 环境均通过。覆盖新旧 header、非法/重复形状、读锁边界、worker 关联歧义、YAML 标签与多行、缓存失效、七角色生成、失败时保留旧目录和审计。
- 真实旧 DSH 0.1.2-rc.1 的 SessionStore + JSONL backend 在独立单会话副本上返回平铺 header，新适配入口读取通过；真实 rc.2 列表在下方全量副本中通过同一入口，253 个 Session 无形状诊断。
- 候选实际由 `@deepseek-ai/dsh-agent-presets@0.1.5-rc.2` 生成 **7/7 角色**；使用目标安装树的 js-yaml 对照，standard 的 **17 个非 persona 行**保持一致，每个角色追加 5 个既有 SilkSecAgent 行。
- **14 份提示资产**（7 个角色、1 个共享调度模板、6 份带 fixture 工作区的调度任务正文）对照生产 manifest 的 191 个动词和 37 个别名，悬空引用 **0**、读取错误 **0**。这不是完整 DSH 模型上下文/实际工具注册验收；后者仍属于 U-C/U-E。

针对性测试命令：

```bash
node --test bundles/dsh/templates/dsh-plugin-sec-suite.host-compat.test.js
python3 bundles/dsh/templates/dsh-plugin-sec-suite.persona.test.py
```

### 9.3 全量 Session 副本结果与阻塞

第一轮运行于 2026-09-13T11:09:13.947+08:00～11:11:03.907+08:00；为排除同一 backend 的缓存命中并补充跨进程锁检查，完善工具后第二轮运行于 **11:16:27.492+08:00～11:18:33.983+08:00**（03:16:27.492Z～03:18:33.983Z）。

| 项目 | 结果 |
|---|---|
| 来源 | data/sessions **251** 个；根 sessions **2** 个。9 月 12 日主目录的 249 是当时数量 |
| 物理格式 | **253/253 为 Session V0**，由日志头及上游目录校验确认 |
| 官方迁移、发布与读回 | **249 成功、4 拒绝**；成功项由官方迁移器处理 Session V0 → Session V1 → Session V2 → Session V3，并只在副本发布 |
| 独立读取 | 成功的 249 个 Session 在新建 backend 中全部读回，事件摘要与迁移后的内存结果一致；ID、创建时间及原 header 中的工作区/父子/预设字段逐项核对 |
| 原件保护 | 253 个生产源文件及副本内的旧代日志，前后 SHA-256 全部一致 |
| 写锁 fixture | 跨进程第二写者被拒；读取与写入并存；读句柄 flush 被拒；只杀死演练创建的空 fixture 子进程后，写锁可重新获取 |
| 放行结论 | **U-B 未通过，不能进入 U3**。尚未覆盖长会话内容人工分层复核、全栈重启续聊、Web/worker 及完整恢复点演练 |

4 个拒绝项均属于生产已有的 Session V0 序号冲突，官方错误为 `SessionPersistenceCorruptionError`。下表 row 是上游报错的记录序号（从 0 计，不含物理 header）：

| Session ID | row | 期望 seq | 实际 seq |
|---|---:|---:|---:|
| `session-04341519-47b6-4dd2-b319-c1e20f67f109` | 305 | 2399 | 2394 |
| `session-1e3f0dd6-0fcf-48f1-9809-34dea9b2ed34` | 715 | 4827 | 4823 |
| `session-52713448-04eb-423e-af66-0638b06ad286` | 398 | 3029 | 3025 |
| `session-52853e3a-d1e3-4637-9a52-09ae49371791` | 295 | 514 | 509 |

四处都发生在旧 `session/end-seed` 后第一条 `tool/result`：前面已有带 error 的工具结果及 step/turn 结束事件，后面继续写入了复用序号的工具结果。已确认是两个不同事件段的冲突，**不能把它当成文件尾截断或简单重编号问题**；实际成因仍需结合当时的恢复/并发写入路径核实。

只读 SQL 检查 findings、tasks、workers、task_runs、reports 的 `session_id` 字段，均未命中这四个 ID；这不证明工作区、日志正文或其他文件没有引用。未删除、截断、重编号或隔离移走生产日志，也未将 249/253 报成验收通过。

第二轮证据保留在 csai 的私有目录：

```text
/opt/silkspool/dsh-upgrades/20260913-rc2/session-rehearsal-JuMo4s/
  report.json          # SHA-256 67a3c04dc27175e1f9d542122296951065e14dd453a0e3c638f94d930e75ffa9
  source-sha256.json   # SHA-256 6d80e9aebe43d949b3ecc5a647130e811e0cacb42498add816fee8b508cf49fa
  source-0/           # 主目录副本，包含保留的 Session V0 与成功发布的 Session V3
  source-1/           # 根 sessions 的副本
  lock-fixture/       # 独立空会话，仅用于写锁验证
```

候选依赖安装日志、17 项测试结果、模板摘要、提示审计、旧版列表兼容结果另存于同级 `evidence/`。这些是专项演练产物，**不是生产统一冻结点备份**；没有生产冻结、数据库恢复或回滚操作可登记。

可重复的专项命令（当前四项未处理前，预期退出码为 1）：

```bash
spool exec csai 'env PATH=/usr/local/node/bin:/usr/local/bin:/usr/bin:/bin node /opt/silkspool/dsh-upgrades/20260913-rc2/candidate/dsh-session-rehearsal.mjs --app-dir /opt/silkspool/dsh-upgrades/20260913-rc2/candidate/app --work-dir /opt/silkspool/dsh-upgrades/20260913-rc2 --source /opt/silkspool/dsh/data/sessions --source /opt/silkspool/dsh/sessions'
```

### 9.4 生产状态与下一步

- 本切片代码基线为 `3b20f08`；提交标题为 `fix: 适配 DSH 会话与角色预设并记录升级预演阻塞`，具体提交可按本文件的 Git 历史查询。10 份变更文档的 139 个本地链接、10 个锚点及 manifest 的 266 个模板路径检查通过；Shell/JavaScript 语法与 diff 空白检查通过。
- **2026-09-13T11:26:29+08:00** 复核：生产 DSH **0.1.2-rc.1**，silksecagent active/running，PID **3683937**，NRestarts **0**。本次没有暂停调度、重启服务、覆盖生产插件或切换版本；37 个兼容别名继续保留。
- 本次“开始执行”的交付边界是方案 §7 建议的首次 Session/persona 兼容切片，另完成了提前暴露迁移风险的 Session 专项；**U1 整包、U2 全矩阵、U3/U4 和 L0–L6 均未宣告完成**。
- 下一步先对四个冲突会话建立可验证的恢复/分支保全方案：逐工具调用核对两段结果、引用和轮次结束语义，在副本上证明无静默丢失，再重新执行全量官方迁移。未经证明，不直接改生产 seq 或跳过拒绝项。
- 可并行推进的工程工作仍为 U1：补齐 app/profiles 的冻结锁与社区插件/patch 清单，加固升级器、冻结写者与完整备份/恢复路径。现有旧 `dsh-upgrade.sh` 仍不满足 Session V3 切换要求，不能直接执行。
- 完整 U2 通过后再切生产并协调更新 setup/version-watch pin；观察期与真实任务周期另行记账。自学习评测、反馈接线和漏洞探测增量仍按 L 工作包验收，不把此次平台兼容测试当成学习效果提升。


## 10. 2026-09-13 续作：分支保全、冻结恢复点与 U2

### 10.1 四个历史 Session 的恢复结论

- 根因已由旧版实际 codec、`interruptedTurnClosers` 和新版 strict 迁移器验证：旧恢复逻辑补写中断结果及轮次结束，随后原执行继续写入同一工具调用的真实结果。压缩分块按官方规则展开后仍冲突；不是计数口径问题。
- 只对精确匹配旧版生成逻辑、调用引用与时间顺序的日志生成两分支。主分支保留真实执行，另一分支保留中断视图；18 条补写记录完整保全。原始压缩文件、两分支原始行及官方发布后的 Session V3 均留存，没有重编号。
- 5 项真实新旧运行时的针对性测试通过；四个会话的两条分支全部经官方 strict 迁移、发布和独立 backend 读回。
- 产物：`/opt/silkspool/dsh-upgrades/20260913-rc2/session-recovery-64PWLv/`；报告 SHA-256 `36f4de4e88a0981a38a24eb998d0c0645e0de6165c5ecf8a73ff721c9d735a16`。
- 2026-09-13T16:45:10.546+08:00～16:47:21.732+08:00 的全量专项为 256/256；正式冻结点随后增加至 257 个，下面使用冻结点重验，不混用旧数量。

### 10.2 完整冻结点与写者恢复

- 新增 `dsh-upgrade-snapshot.py`：全树字节、属主、权限、时间、扩展属性及树内硬链关系核验；独立 SQLite backup 镜像及 integrity_check；只恢复到新目录。
- 新增 `dsh-upgrade-freeze.py`：检查 tasks/workers 排空，记录并暂停相关 cron/timer/服务，检查进程写者，再创建完整恢复点；默认自动恢复原活跃单元与原 cron，失败同样恢复；原先 inactive 的 oneshot 不被额外启动。
- 7 项文件/WAL/冻结回归在管理机和 csai 通过。开发中修复了“原目录消失后无法恢复”和“SQLite 镜像清单缺项未拒绝”两处问题；测试覆盖证据损坏、并发写入、已提交 WAL、硬链/软链保全及冻结失败恢复。
- 冻结作业开始 **2026-09-13T18:51:08.556624+08:00**；全部写者停止并确认空闲为 **18:52:40.715233+08:00**；快照独立校验完成 **19:02:08.144152+08:00**；原活跃服务和 cron 恢复 **19:02:09.306665+08:00**，恢复错误 0。
- 冻结包含 14 个明确单元、root/silkspool cron 检查（silkspool 两条任务暂停后原样恢复）。恢复后的生产基础验收 **25/25**；仍运行 **0.1.2-rc.1**，MainPID 3734437。这是备份窗口的重启，不是版本切换。
- 恢复点：`/opt/silkspool/dsh-upgrades/20260913-rc2/dsh-snapshot-ready-ujs97n3y/`；manifest SHA-256：`78395cbbe11247f7623b100081d7913cda80b19065e7ebc7a96b513646b98986`。
- 六个归档根：dsh 全目录（73,989 个文件系统条目，含 Session/results/flows/依赖锁/插件及备份）、三个外部工作区（1,603 / 1,471 / 113 条目）、Node 运行产物（6,967 条目）、原写者配置（20 条目）。没有笼统排除证据目录。
- `asset-graph.db` 独立镜像涵盖 44 张表，integrity_check=ok；镜像 SHA-256 `b7d8d7749ba277039e7e2e0c3b25c7fe2037772813de135ee02ba4d4debb970e`。其他文件型库随全树冻结保全。

### 10.3 旧版恢复与冻结点的 rc.2 Session 预演

- 原文件树恢复到 `/opt/silkspool/dsh-upgrades/20260913-rc2/dsh-restore-copy-20r5xij9/`，逐项校验完成后，使用独立 mount/user/PID/network namespace 绑定副本到原绝对路径。宿主其余文件系统只读，生产 .env 被屏蔽，应用按 silkspool 身份运行；没有项目出口。
- 旧版真实 Web/headless 配置组合、测试身份登录、BrowserAuth cookie、应用页面、15 个业务域/总线后端、3 个工作区的 175 条关联 Session 列表，以及未登录 RPC 拒绝均通过。报告位于 `dsh-sandbox-mr67pujz/runtime-report.json`；没有调用模型。
- 预演初版错误地把总线 `sidecars=false` 当成纯后台开关，它实际表示 agent 面并停止提供宿主门面，导致 14 域未注册。延长等待不能修复；保留宿主门面、只延后 dispatcher 后通过。此项是隔离工具配置问题，未修改生产总线。
- 原封恢复的 257 个旧日志中，253 个可由 0.1.2-rc.1 实际读回，另 4 个仍是升级前已有的同一序号冲突。给**隔离回滚副本**安装已保全的旧格式执行分支后，旧版全量 **257/257** 读回；原始冲突文件留在独立归档中。回滚方案必须携带这四项恢复配方，不能只声明原始日志天然全部可读。
- 旧版完整读回：`dsh-sandbox-z92dfnlo/session-read-report.json`；读前/读后 257 个日志字节校验一致（四个恢复分支按各自 stored_sha256），证明文件为 `evidence/u2-legacy-read-integrity.json`。
- 对完整冻结点重新执行 rc.2 官方迁移：**257/257** 通过，四项恢复均应用，4 项写锁测试通过，冻结点原件哈希未变。报告：`session-rehearsal-hBXcLb/report.json`。

### 10.4 候选与仍待验收项

- 新静态候选：`/opt/silkspool/dsh-upgrades/20260913-rc2/dsh-candidate-_k4tn_ua/`。app 维持显式 0.1.5-rc.2；web/headless 的 file/link 依赖改为候选内部相对路径，核心包 override 对齐目标 app 的实际版本，autoInstallPeers=false。
- 两个 profile 均完成普通候选锁生成及 **--frozen-lockfile --offline** 重装，锁哈希不变；七个 persona 从 rc.2 shipped standard 生成。社区包仍须通过真实运行验证，peer warning 不作为兼容通过。
- rc.2 全栈启动、原模型配置/补丁兼容、实际 worker 与 Web 操作等继续执行；候选报告 `ready_for_cutover=false`。
- U3 生产版本切换、U4 至少 72 小时观察及 L0–L6 学习增量尚未完成。本节不会把专项测试或基础健康检查扩大为整包验收。

### 10.5 rc.2 真实运行适配与 worker

- Cordis 的 connection 提供者必须显式注入 `webRuntime` 和 `webServer`；只修改 RPC 消费者不能解决加载顺序。sec-suite 与 bus 的 RPC 注册现在随 fiber 生命周期正确安装和释放。
- 统一 worker 运行入口：关闭 stdin、共享并同步 stdout/stderr 文件描述符、按进程组取消/超时回收；通过随机启动 nonce、PID、工作区和官方 Session header 核实子会话。
- `workers.session_id` 保持原有来源会话语义；新增 `worker_session_id` 保存子会话。结果及 meta 的子会话 ID 与 origin 分开，避免破坏历史归因。
- bus 在长 worker 执行前提交启动事件，不跨执行持有 SQLite 事务；规范工具与保留别名都传递 cwd、超时和 AbortSignal。原生 bash/web_fetch 不允许绕过 Scope/exec；write/edit 限物理工作区并保护平台和执行控制文件。
- 七角色的实际模型请求中，最终 prompt、工作区与工具指导通过：`dsh-sandbox-13z6yw67/runtime-report.json`。真实 worker 完成、幂等重试、失败、超时、在线注册、原生 Session 取消通过：`dsh-sandbox-27znvxtk/runtime-report.json`。

## 11. 2026-09-14 续作：Web、浏览器与全量回归

本节产物均位于 `/opt/silkspool/dsh-upgrades/20260913-rc2/`。除 §10.2 已记录的备份窗口外，生产尚未切换；11:11:43 UTC 复核仍为 MainPID 3734437、NRestarts=0、active/running、FreezerState=running。

### 11.1 Web 文件交付与计费兼容

- 根因一：dsh-bill 0.13.1 在首个匹配即占用的 `conversation.chat.turnTail` 槽认领了每个已关闭轮次，即便不显示费用也遮住官方文件卡片。费用移到已有 additive `conversation.chat.assistant-actions`，按最终 messageId 关联轮次，保留官方交付和预览实现。
- 根因二：该插件的旧投影定义使用 `schema/view`；rc.2 需要 `stateSchema/wire.viewSchema/wire.view`。旧定义成为仅宿主可见，客户端收不到 billTurns。现补齐持久化状态校验、发布 wire 投影并将 stateVersion 从 1 升到 2。
- 所有运行时补丁同时限制版本和完整文件 SHA-256，未知产物直接拒绝。计费客户端结果摘要 `5c5f9d6518c8aa40bce291b5c0fafd810b9f28b28a7b31ffdb6359d3df5d87de`；投影结果摘要 `77a63d6540b20a15a36873dd7686fd9404006aa4c67a29efceb7674e6d4cdf24`。
- 最新实际验收：`dsh-sandbox-jqwtpcmo/runtime-report.json`。完整 Caddy edge 登录/BrowserAuth、10 视图、设置保存、主题显式切换、流式工具调用、Session 刷新、WebSocket 断线重连续聊通过；使用每轮唯一文件名，验证文件卡片、预览与费用同时可见，并刷新读回。费用精确核对 3 次调用、48 input / 12 output tokens、USD 0.000096。

### 11.2 Browser fork 的 Scope 强制出口

- 旧 fork 在真实 `browser_open` 中能直接到达越界 fixture，故 U-G 不能只检查原生 web_fetch。新增受管 HTTP/CONNECT 代理，复用 scope 域的授权匹配与 active 风险规则；浏览器重定向、页面 fetch、WebSocket、Service Worker 均经过它，再转发到原 `SEC_FLOW_PROXY`，上游失败不回退直连。
- Chromium 禁止 QUIC 与非代理 WebRTC UDP，取消回环地址的代理绕过。CDP 只连接本机，核验真实启动参数、Scope 路径与守卫代码摘要；未受管浏览器拒绝接入。截图共用原生写文件的物理工作区边界。
- Playwright 1.62.1 默认未带 `--enable-automation`，导致启动参数无法核验，现由受管参数显式加入。共享宿主的两套信号处理曾同时关闭 Chromium，强杀导致 cookie 未落盘；现统一正常关闭。CDP close 正确断开客户端并清理引用，不终止共享宿主；同 manager 可在关闭或宿主重启后重连。
- 12 项真实 Chromium/CDP 验收通过：`dsh-sandbox-govy20gj/browser-scope-report.json`，越界目标到达 0。覆盖 direct/redirect/eval/WS/Service Worker 拒绝、允许访问、截图边界、共享 CDP/代理/登录态、重启保持 cookie、HTTPS CONNECT 和未受管 CDP 拒绝。实际模型发出的 browser_open 也被拦截，见 §11.1 同一 runtime 报告。
- fork 固定 `0.1.0-silksec.2`、playwright-core `1.62.1`、schemastery `3.18.2`；确定性 tarball SHA-256 `ac2a183f8c1f27f1c791d61a37643e55f9e74d9b499473e5a386704cbd92cc70`。Web 冻结锁摘要 `25536eba614d5df83424431d3481f5452b4bab4422811f04d1b803f5a0c4bba9`，离线冻结重装及安装字节一致性通过。尚未安装到生产。
- 当前代理的速率计数在 HTTP 请求/CONNECT 建链层，未声称逐个统计 HTTPS 隧道内请求；域名授权沿用既有 scope 语义，没有额外宣称 DNS 固定或重绑定防护。

### 11.3 全量回归与尚未放行条件

- 最新全部领域契约 **352/352**，15 域、16 个测试文件；owns×sandbox **67 项、14 业务域、0 违规**：`dsh-sandbox-4ya29vtl/contracts-report.json`。
- 抽取共享文件守卫后，真实 headless 的 11 项原生工具/文件边界回归通过：`dsh-sandbox-df8ejgo7/worker-report.json`。
- 原冻结点的模型配置在 rc.2 双 profile 组合和真实 Web 加载通过：`dsh-sandbox-2q9yfzlb/runtime-report.json`。此项没有调用真实模型，不能替代真实路由、失败切换或 reasoning 补丁必要性验证。
- 冻结工具仍需消除“检查空闲后再次派单”的竞态并覆盖原生 Session 排空；安全切换/回滚工具、U-H 数据/证据核对、最终候选封存与完整升级→回滚演练仍待完成。
- U-G 尚需完成模型失败切换、实际出口、反馈不外传等验证。U3/U4、L0–L6 未完成，未提交或推送这次尚未完成的整体任务。

## 12. 2026-09-14 续作：反馈、实际出口与发布恢复工具

本节证据仍位于 `/opt/silkspool/dsh-upgrades/20260913-rc2/`。生产保持 0.1.2-rc.1、MainPID 3734437、NRestarts=0；没有执行版本切换。

### 12.1 模型、反馈及维护入口

- 双 profile 显式配置 `session-telemetry-otel.mode: DISABLED`，关闭 `session-log-deepseek` 和 `plugin-package-inventory-deepseek`。真实 RPC 创建、修改、删除反馈，独立 canonical 读回两次 put/一次 delete；Session/message 归因正确，后续模型请求未带入反馈，外传请求 **0**：`dsh-sandbox-a2nmsr6n/runtime-report.json`。
- 正向对照显式启用 FEEDBACK_ONLY，仅向隔离本机 collector 发送；检测器观测到 **2** 次请求：`dsh-sandbox-6rvz7i7s/runtime-report.json`。验证中发现 Cordis patch 会替换整段 config；只覆盖 exporter 会丢失 DISABLED。已修正测试覆盖，正常验收直接检查实际组合的部署配置。
- 失败切换与投影缓存：`dsh-sandbox-fxw5vva4/runtime-report.json`、`session-trace.json`。原模型能力 fixture 的 max→high 与 reasoning_content 历史重放通过：`dsh-sandbox-x8eqz9o3/runtime-report.json`，未追加旧 reasoning 补丁。
- 实际 Bellkeeper/pi-ai 流式调用成功，保留 `pool-secagent` 及既有路由，单次有界请求，maxTokens=128，实际 100 input / 42 output tokens；约 1995 ms，结果为指定连通性答案，仅记录摘要：`dsh-egress-zoofdih4/report.json` 的 `actual-bellkeeper-pi-ai-stream`。该报告总结果 false 来自其他网络项，不把整体报告误记为全部通过。
- 旧版/rc.2 本机维护客户端的 idle→running→cancel→idle、临时账号恢复和注销分别通过：`dsh-sandbox-bcu938cm/runtime-report.json`、`dsh-sandbox-bxcckg7p/runtime-report.json`。生产 journal 凭据后备路径仍待实测。
- 独立临时 systemd 服务确认 freeze 停写、thaw 恢复、stop 回收冻结进程：`dsh-freezer-smoke-qf6sxmnr/report.json`。此项没有冻结生产服务；完整冻结器仍须携带已核对的 scheduler 摘要完成集成验收。

### 12.2 浏览器拒绝归因与出口实测

- 多次实际共享 Chromium 成功取得 Example Domain，页面无失败请求，却因浏览器更新/登录后台流量被 Scope 拒绝而报页面失败。对应 `dsh-egress-950a2v35`、`dsh-egress-r6582fe6`、`dsh-egress-stzxxedy`。显式关闭后台网络的 Chromium 参数不能独立解决此问题。
- 回归用例在原候选上稳定失败：`dsh-sandbox-3jpd1j_c/browser-scope-report.json`。现在代理保留有界的拒绝序列及 origin 摘要，manager 将其与本次 BrowserContext 请求、WebSocket 关联；不再使用全局计数直接判断页面失败。日志窗口溢出则明确拒绝，Scope 代理继续拦截全部请求。
- 首轮修复的 WebSocket 回归拒绝报告缺失，定位到 Chromium 通过 CONNECT 承载明文 ws；代理只看到 authority，已按实际端口补齐关联。定位证据 `dsh-sandbox-ok3fzafi/browser-scope-report.json`；临时探针已从模板删除。
- 最终 **15/15** 实际 Chromium/CDP 检查通过：`dsh-sandbox-hjh0gzhd/browser-scope-report.json`，越界到达 **0**。在原 12 项上增加普通/共享浏览器后台拒绝隔离及页面限速拒绝报告；WebSocket、Service Worker、重定向、截图边界、CDP 登录持久化均通过。代理的 5 项本地测试同时覆盖有界脱敏日志与既有连接授权撤销。
- 当前 fork tarball SHA-256：`9d9533110c71aa7d233fa7f026f59fcb2274e219bd8c61340a453a28aa6d2837`，Web pnpm 锁：`5f59ec11d354eae9a4c5e9c0018934a69d42a30671a3e34e9ba0d6efe46714e1`；仍为未发布的 `0.1.0-silksec.2`。`plugins.lock` 已同步，§11.2 保留此前验收产物的摘要。
- 真实共享浏览器通过既有 7777 上游取得 HTTP 200 / Example Domain，页面请求失败 0，越界入口拒绝：`dsh-egress-jlevrmsh/report.json`。此前 `dsh-egress-9jlo62t_` 返回 X-Ray 错误页，保留为失败证据。
- 实际 fetch 按现有环境直连成功。CLI 通过既有 mubeng 8899 成功，连接对象为 127.0.0.1:8899，总请求约 7.32 s：`dsh-egress-jeuya8l5/report.json`。此前成功与 25 s 超时均保留（`dsh-egress-yso48tue`、`dsh-egress-zoofdih4`）；这些结果证明路由可用，不代表现有代理池长期无波动。

### 12.3 发布与数据不变量预演（进行中）

- `dsh-upgrade-release.py` 提供候选封存、完整 next/rollback 树准备、Linux `RENAME_EXCHANGE` 切换及日志恢复。9 项真实目录交换测试通过，覆盖交换后未记日志、部分切换/回滚、篡改拒绝、原件及新增状态保全。生产切换要求持有冻结点及完整 U-A～I 验收；观察阶段不允许直接用旧冻结点自动覆盖新增业务。
- `dsh-upgrade-invariants.py` 检查 44 张业务表的行、主键、schema，以及证据、Scope、设置和外部工作区。只允许确切的 nullable `workers.worker_session_id TEXT` 追加与指定浏览器宿主更新；同域版本的启动观测时间单独归一。8 项真实 SQLite/证据测试通过，包含旧列约束变化与伪造历史归因的拒绝。
- 新鲜恢复副本 `dsh-restore-copy-yzcuyarp` 的基线为 `evidence/u2-invariants-baseline-v2.json`。后续必须实际完成升级、启动、不变量比较、完整回滚、旧版启动与全量读回；尚未将准备动作计为恢复验收。
- setup 显式锁 rc.2，在跨版本时于变更前拒绝；保留已有模型设置。关键插件及兼容步骤失败即退出。新增恢复/验收模板已加入 bundle manifest；生产中的旧升级脚本尚未覆盖，不能调用旧快捷升级。
- U3/U4 与 L0–L6 仍未完成。真实 ≥72 小时观察不能由副本测试替代；本次整体任务尚未满足提交条件。

## 13. 2026-09-14 续作：完整回滚、U-H 差异归因与索引修复

### 13.1 维护入口与完整交换

- 生产 journal 认证后备已实测通过：`dsh-maintenance-auth-5b4frwxb/report.json`，260 个原生 Session、0 running，临时用户文件恢复并注销。客户端按当前 MainPID/InvocationID 选择最新启动链接，拒绝启动竞态；3 项回归通过。
- `dsh-release-0zbt6p_7` 在绑定副本 `dsh-restore-copy-yzcuyarp` 上完成 next/rollback 整目录交换。rc.2 最小维护启动为 `dsh-sandbox-ytdp4lr3/maintenance-report.json`；随后完整回滚并启动旧版，`dsh-sandbox-tghlqfle/maintenance-report.json` 通过认证、15 域、3 工作区、175 条关联 Session、Scope 越界拒绝及身份清理。
- 回滚后旧版 **257/257** 全量读回：`dsh-sandbox-7_fge9jg/session-read-report.json`。主目录 255 个与另一根 2 个分别统计，不把原生主目录列表数量当作全部日志数量。
- 发布工具已有 14 项 root 测试，冻结器 7 项；恢复写者之前必须有当前最小启动与业务不变量证明。先持久化 `resuming`，再启动写者，防止中断后错误地使用切流前回滚路径。恢复写者后要求另取完整冻结点、保全新增 Session/业务数据并生成对账报告；不自动丢弃新数据、不承诺零 RPO。
- 维护工具使用实际 `web --host 127.0.0.1 --port 3081` 入口，只关闭后台派单、探针和外传；保留原配置与未知 YAML 标签。真实隔离启动已通过；生产临时 systemd drop-in 的集成执行尚未发生。

### 13.2 U-H 的原始失败与对照

- 原始失败保留在 `evidence/u2-release-next-invariants.json`；逐项诊断为 `evidence/u2-invariant-diagnostic.json`，旧版启动对照为 `evidence/u2-invariant-rollback-diagnostic.json`。两个版本都出现相同的启动副作用。
- outbox **3597 → 3612**，3597 条原记录逐字段未变；新增恰好 15 条 `bus.domain.registered`，全部 delivered。事件文件原始 **781515 字节**完整作为前缀保留，追加同一组 15 个事件；不是任务执行或工作区绑定记录。
- `bus_meta` 只有域版本观测时间及 `replay.watermark.updated_at` 刷新，watermark 值未变。三个 Program 的观测时间刷新，其中一个 `platform` 去掉了旧镜像多存的外层引号，结果与未改变的 `scope.yml` 一致。授权、状态、工作区 ID/path 均未变。
- `kb_fts` 的 **392** 条 rowid/标题/正文完全相同，FTS 内容一致性检查通过；只有物理索引布局改变。
- 更严格的 `exp_fts` external-content 检查发现**升级前已存在**的索引失配：普通 SQLite integrity_check 与索引内部检查均通过，但 `rank=1` 与原表校验失败。原件对照为 `evidence/u2-fts-original-control.json`；官方 `rebuild` 在临时副本修复，**40 条经验原文未变**。

### 13.3 修复与门禁

- 索引写入顺序有真实复现：同场景合并先更新 `exp_cards`，随后 external-content FTS 取到新文本，旧词留在倒排索引。新增实际域调用回归在原代码上失败（`dsh-sandbox-js21on18`，原 352 项通过、新增 1 项失败）。
- 新后端先用旧文本删除索引项，在同一 savepoint 内更新原文并写入新索引；归档在移除原文前清理索引。旧兼容入口同样修正，并补齐打法链自动登记遗漏的索引及索引文本与原文不一致的问题。失败回滚保留原状态，没有新增表或第二套真相源。
- 最新全量领域契约 **354/354**，owns×sandbox **67 项、14 业务域、0 违规**：`dsh-sandbox-1_nvzhbh/contracts-report.json`。兼容经验入口在实际 Node 22 上通过；安装脚本 3 组执行测试通过，覆盖跨版本变更前拒绝、保留既有模型设置及关键步骤失败不重启服务；系统命令使用隔离替身，不声称完整空机安装验收。
- 不变量报告升级至 schema **3**；基线 `evidence/u2-invariants-baseline-v3.json`，SHA-256 `30d0aee35afd55cedecdf4a5408ea969b496d34d0e56246450da3ec6415ce405`。15 项 SQLite/证据测试通过。新门禁逐条保护既有 outbox、校验注册事件与原日志前缀；只允许与未变 Scope 对应的去引号和观测时间刷新。FTS 物理索引变化必须以 rowid/正文相同且内容一致性通过为前提。
- 在尚未修复的真实 rc.2 副本上，新门禁仍准确拒绝 `exp_fts.content_integrity`，其他差异得到上述严格归因：`evidence/u2-unrepaired-index-gate.json`。没有直接豁免学习索引。
- 发布准备仅在 next/rollback 副本使用 SQLite 官方重建失配索引，事务内逐表核对业务行并记录两份修复报告。完整原冻结点保持原样。新鲜副本 `dsh-restore-copy-asrhrri9` 已恢复并开始整套重验，尚未据此宣布 U2/U3 完成。

### 13.4 2026-09-15 整套重验：升级→不变量→回滚→旧版读回

在绑定冻结点 `dsh-snapshot-ready-ujs97n3y`（manifest SHA-256 `78395cbbe11247f7623b100081d7913cda80b19065e7ebc7a96b513646b98986`）的全新副本 `dsh-restore-copy-asrhrri9` 上，一次性跑完加入全文索引修复后的完整六步重验。发布目录 `dsh-release-o_ao_rh0`，工具为 `recovery-tools/` 下与仓库 `bundles/dsh/templates/` 逐文件 SHA-256 一致的六个脚本。全部产物汇总于 `dsh-reverify-u2u3/`，其 `evidence-manifest.json`（SHA-256 `37c9bc8a6bf43a0969a62faa6d4c182d1954deb4ac7ef85950420b52b53424e8`）逐项列出九份证据哈希。

| 步 | 动作 | 结果与证据 |
|---|---|---|
| 1 | `switch`：候选封存校验、四根当前树逐条目对齐冻结点、next/rollback 清单核对后 `RENAME_EXCHANGE` | 四根全部 `switched=true`，副本 app 读回 **0.1.5-rc.2**；生产 app 仍 **0.1.2-rc.1**，`release-state-final.json` |
| 2 | rc.2 隔离最小启动（bwrap：宿主只读、屏蔽生产 .env、私有回环、silkspool 身份） | `version=0.1.5-rc.2`、15 域、3 工作区、原生 255、关联 Session 列表 **175**、Web 应用、Scope 越界拒绝、未认证 RPC 拒绝、身份恢复并注销：`rc2-maintenance.json` SHA `339312db4d9fe11a4df02f6fe2602120d39fad1acd0c44a733e80260f053b449` |
| 3 | `dsh-upgrade-invariants.py` 对照 v3 基线 | **ok=true、failures=0、44 表**，业务行/主键保全；差异逐项归因见下：`u2-rc2-reverify-invariants.json` SHA `18b639e845a005631711895f763983fb67441ccbf29320f6736031d639bd31e2` |
| 4 | `rollback`：`RENAME_EXCHANGE` 回切 | `phase=rolled-back`、`new_state_preserved=true`、副本 app 读回 **0.1.2-rc.1** |
| 5 | 旧版隔离最小启动 | `version=0.1.2-rc.1`、15 域、3 工作区、原生 255、关联 Session 列表 **175**、Web/Scope/未认证/身份清理全绿：`rc1-maintenance.json` SHA `44aa97f5722369304c7309146ab396a411fdfc133e2ab94c8af0075016248883` |
| 6 | 回滚副本旧版全量读回（4 个恢复配方随 rollback 树携带） | **257/257、failures=0**（`data/sessions` 255 + 另一根 2），顺序只读延迟 p50 40.8 ms / p95 108.3 ms / max 230.1 ms：`rc1-session-read.json` SHA `8f921560612033b2dd240496fd1b4ff447c2bd2a873450fc6daa9d15a1c5ed76` |

第 3 步不变量差异只落在既有允许类别，无非预期变化：`startup-registration-outbox:15` 与 `startup-registration-log-append:15`（启动期 15 条 `bus.domain.registered`，仍受 §13.2 的前缀与逐字段保护）、`startup-mirror-observation:bus_meta`/`programs`（仅观测时间刷新）、`nullable-worker-session-id-column`（`workers.worker_session_id TEXT` 追加）、`managed-browser-host-update:workspace-2`（候选覆盖受管宿主）、`repaired-existing-fts-index:exp_fts` 及 `verified-fts-physical-index` 的 `exp_fts_data/docsize/idx`、`kb_fts_data/idx`（物理索引重建，rowid/正文一致为前提）。修复后的 `exp_fts` 这次不再触发 `content_integrity` 拒绝，与 §13.3 门禁一致。

验收口径与边界：本次是隔离副本的重验，不等于 U3 生产切换，也不替代 ≥72 小时生产观察；「关联 Session 列表 175」是 Web/看板按工作区关联口径的计数，与全量日志 257（原生 255 + 根 2）分别统计，不互相冒充。生产全程 `0.1.2-rc.1`、MainPID 3734437、NRestarts=0、active/running，未暂停、未切换、未改动写路径。据此 U2 的升级/不变量/回滚/读回闭环放行；U3、U4 与 L0–L6 仍未执行。

## 14. 2026-09-15 U3 生产切换：冻结→切换→门禁→恢复写者

U2 整套重验（§13.4）放行并经用户明确批准后进入 U3。生产切换使用**当日新鲜冻结点**而非 9 月 13 日的旧冻结点：9 月 12 日基线记录的 249 与 13 日的 257 只是当时点数，切换窗口内生产已增长，旧冻结点不能覆盖新增 Session。全程产物位于 `/opt/silkspool/dsh-upgrades/20260913-rc2/`，发布目录 `dsh-release-2non9v70`。

### 14.1 切换过程中发现并修复的两个工具缺陷

这两处都在 P3 生产最小维护验收阶段暴露，属于**维护工具自身缺陷**，不是生产数据被破坏；每次失败都走 `finally` 的 `cleanup`，落盘失败报告并保持写者 held，未产生半成品状态。

| # | 症状 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| 1 | `维护命令含未支持的 systemd 转义` | `dsh-upgrade-maintenance.py` 的参数字符白名单 `[A-Za-z0-9_./:=+-]` 缺 `@`，而真实入口二进制位于 `…/node_modules/@deepseek-ai/dsh/lib/bin.js`。`@` 在 systemd `ExecStart` 中本就无需转义 | 提取为常量 `SAFE_SYSTEMD_ARG`，白名单补 `@`；新增回归用例同时断言 `@scope` 路径通过、`; " ' \ $ \` # 空白` 等注入字符仍被拒 | `dsh-upgrade-maintenance.test.py` 6/6 |
| 2 | `journalctl … returned non-zero exit status 1` | `dsh-upgrade-local-client.py` 的 `current_launch_url` 对 `journalctl --grep` 传 `check=True`；`--grep` 无匹配时按设计返回 1（启动轮询竞态），抛 `CalledProcessError` 绕过重试循环里只捕获 `RuntimeError` 的分支 | 改为 `check=False`；无匹配时 stdout 为空，由 `journal_launch_url` 抛出可重试的 `RuntimeError`， Genuine journalctl 故障仍表现为读不到凭据并在 60 s 超时后失败 | `dsh-upgrade-local-client.test.py` 3/3；生产 `browser_auth_source: journal` 实测走通 |

修复后模板 SHA-256（本地 `bundles/dsh/templates/` 与 csai `recovery-tools/` 已一致）：`dsh-upgrade-maintenance.py` `ec27579d1bf11fa1e8749865d344bdedca1c8ffdb6bd76c0907a33e70e1c4d9d`；`dsh-upgrade-local-client.py` `4082bf5e7609b20b97d9403c8005bdb3ddc3f207b320219a7776f2f00fa499ca`。

同步边界（诚实记录）：本次以单文件方式把修复写入本次发布的权威工具目录 `recovery-tools/`，因为 `spool bundle dsh setup csai` 会重启服务，在冻结/观察窗口内不能执行。`rsync -a bundles/dsh/ /opt/SilkSpool/bundles/dsh/` 与 bundle setup 的运行时副本同步**推迟到 U4 观察期满后**执行；在此之前不得用 `spool bundle dsh` 的旧运行时副本重新生成 `recovery-tools/`。

### 14.2 切换流水

| 步 | 动作 | 结果与证据 |
|---|---|---|
| F1 | `freeze capture --hold`：排空 tasks/workers、暂停 14 单元与 2 条 silkspool cron、原子空闲核验、建完整恢复点 | 冻结起始 `12:17:58Z`、原子空闲 `12:19:35Z`、冻结完成 `12:19:37Z`、快照独立校验 `12:33:01Z`；恢复点 `dsh-snapshot-ready-8jhp928l`，manifest SHA `fdeea4b34fce80a2f2c742d6caa6bccb170449bc9b62f005d4b1ab1aaac5a0cd`；原生空闲 263 Session / 0 running；状态目录 `dsh-freeze-ksshetzv` |
| F2 | 候选封存 | `dsh-candidate-_k4tn_ua`，`ready_for_cutover: true`，seal SHA `97f0010a52fa0a5d77f4060bac4fca13f49c4036446b2d7af7ed2cf0349e0c4d` |
| P1 | `release prepare`：next/rollback 全树 + 官方 Session 迁移 + 4 恢复配方 + 索引修复，绑定验收文件 | `phase=prepared`、`mode=production`、`restore_copy=null`；迁移 **265** Session（`data/sessions` 263 + 根 2）ok/failed=0，报告 SHA `247016c18b4842551e25bb2514f510c78181a5d8aa7605962e7eb527d6485654`；4 项 lock_checks 通过、`legacy_recoveries=4`、next+rollback 双侧 `index_repairs`；`prepared_at=13:44:03Z` |
| P2 | `release switch`：四根 `RENAME_EXCHANGE` | 四根 `switched=true`，`phase=switched`，`writers_held=true`，`switched_at=13:54:14Z`；`/opt/silkspool/dsh` 读回 **0.1.5-rc.2**，旧树保全于 `rollback/` |
| P3 | `maintenance smoke`：临时 systemd drop-in 启动 rc.2，只静音调度/探针/外传，跑最小验收后停服并删除 drop-in | `ok=true`；`version=0.1.5-rc.2`、15 域全注册、3 工作区、原生 **263**（0 running）、关联 Session 列表 **183**、Web 应用、Scope 越界只读拒绝、未认证 RPC 拒绝、身份恢复并注销；报告 `maintenance-switched-report.json` SHA `cf718b2a3ec2dd472df748a40c9d86ed5facd12acbd8f33883a145937b1f0a5a`，`maintenance-state.json` SHA `be3931f80ed0af14d36d4f75d6147c5a89798583aa25ba70b6e154f989759ff4`（`cleaned_at=14:12:00Z` 与报告 `started_at` 对应）；静音 8 行，patch SHA `271911669f6b9a76d4a31a518eeafc8ba8a43ec2d911d5c9eacdfc64812c35b8` |
| P4 | `release finalize`：先跑生产 `pre_resume_invariants`，通过则持久化 `resuming` 再恢复 14 单元 | `phase=observing`，`writer_resume_started_at=14:16:33Z`，`observation_until=2026-09-18T14:16:35Z`；不变量报告 SHA `0ddb25b0ad45783593f0b6364e6b8bffa2fa7062a8ea666405392041c0ddfc68`；`state.json` 切换后 SHA `1704dea63fa5c31ca494112e0818c766627cac41a8708f39f06857c753e74f07` |
| V1 | 恢复后基础复核 | 服务 `active/running`、MainPID **3848865**、NRestarts **0**、FreezerState `running`、app 读回 **0.1.5-rc.2**；10 个服务/timer 单元全部 active；2 条 silkspool cron 无 `# dsh-upgrade-paused` 残留即原样恢复；`scheduler.lock` 14:20 刷新。journal 可见调度循环 60 s tick、vault 回流 `imported=4/errors=0`、memcore sweep 正常 |

### 14.3 生产不变量差异归因

P4 的 `pre_resume_invariants` 以冻结点 `trees/`（`before`）对**持有中的生产实路径**（`after`，`mode=live-held`）比较，两侧 manifest SHA 均为 `fdeea4b3…`。结果 `ok=true`、`failures=[]`、`business_rows_and_keys_preserved=true`、**44 张业务表**行/主键保全，`diffs=[]`（无非预期变化）。

被门禁显式归类的 `expected_changes` 共 12 项，全部落在 §13.3/§13.4 已建立并测试过的允许类别，不含任何新类别：

- `startup-registration-outbox:15`、`startup-registration-log-append:15`：P3 启动期新增 15 条 `bus.domain.registered`；既有 outbox 记录逐字段未变，事件文件保持原字节前缀，新增项按域目录 payload 与时间戳校验。
- `startup-mirror-observation:bus_meta`、`startup-mirror-observation:programs`：仅域版本/程序观测时间刷新及 `replay.watermark.updated_at`，watermark 值与授权、状态、工作区 ID/path 未变。
- `nullable-worker-session-id-column`：`workers.worker_session_id TEXT` 的可空列追加（`ensureCol` 幂等演进，未改表名、未迁库）。
- `managed-browser-host-update:workspace-2`：候选携带的受管浏览器宿主文件更新。
- `repaired-existing-fts-index:exp_fts` 与 `verified-fts-physical-index` 的 `exp_fts_data`/`exp_fts_docsize`/`exp_fts_idx`、`kb_fts_data`/`kb_fts_idx`：P1 在 next/rollback 树内以官方 `rebuild` 修复的既有物理索引；rowid/正文一致与内容一致性检查为前提，修复报告 `next-index-repairs.json`/`rollback-index-repairs.json` SHA 同为 `ed6464e5d3c573941a1265052fd7302c646d452d649ad2cf79a8ec556ecf5cab`。

修复后的 `exp_fts` 未触发 `content_integrity` 拒绝，与 §13.3 的门禁行为一致。

### 14.4 观察期与尚未完成项

- **U4 观察期**：`2026-09-15T14:16:35Z` 至 `2026-09-18T14:16:35Z`。观察期内要求：另取完整冻结点、保全切流后新增 Session/业务数据、生成 `preserve_after_resume` 对账报告；不自动丢弃新数据，不承诺零 RPO。37 个兼容别名的观察期至 `2026-09-19T15:23:24.420+08:00`，未到期不删除。
- **回滚窗口**：rc.2 若需回滚，必须走 `dsh-release-2non9v70` 的完整冻结点恢复 + `RENAME_EXCHANGE` 回切，不能只改 `package.json`；`rollback/` 树已保全 `0.1.2-rc.1` 原件与 4 项旧格式恢复配方。Session V3 切换后旧版只能读取 §10.1 已保全的执行分支。
- **推迟动作**：`rsync -a bundles/dsh/ /opt/SilkSpool/bundles/dsh/` 与 `spool bundle dsh setup csai`（§14.1），以及 `setup`/`version-watch` pin 的运行时生效核对。
- **未执行**：≥72 小时观察结论、切流后新增 Session 的迁移与读回对账、真实任务周期下的 worker/模型路由长期稳定性、L0–L6 学习与漏洞探测增量。以上均**不得**以 §14.2 的 P3/P4 结果替代。
- **口径边界**：P3 的「关联 Session 列表 183」是生产 Web/看板按工作区关联口径的计数，原生 Session 为 263，与 §13.4 副本上的 175/255 分别统计，不互相冒充；`smoke` 是维护模式的启动与只读业务验收，未调用真实模型、未派发真实任务，属于基础健康检查而非整包升级完成。

至此 U3 生产切换的六个动作（F1/F2/P1/P2/P3/P4）全部通过并有独立证据哈希，生产运行 **0.1.5-rc.2**；U4 观察进行中，L0–L6 未执行。

### 14.5 U4 观察期首次巡检（2026-09-15T14:57Z）

**观察期未满，不执行关账。** 本次为观察期内的首次只读巡检，产出证据快照，不替代 ≥72h 生产验证结论。巡检时距观察期满（`2026-09-18T14:16:35Z`）还剩 **71.4 小时**。

| 项目 | 实测结果 |
|---|---|
| 服务 | `silksecagent.service` active/running；MainPID **3848865**；NRestarts **0**；FreezerState `running`；切换后持续运行 |
| 版本读回 | `/opt/silkspool/dsh/app/node_modules/@deepseek-ai/dsh/package.json` = **0.1.5-rc.2** |
| `state.json` | `phase=observing`；`observation_until=2026-09-18T14:16:35.602762+00:00` |
| journal（启动至今） | 31 行；15 域全部 `registered=true`；无 fatal/panic/OOM/unhandled；onnx `pthread_setaffinity_np` 警告为容器已知行为，非崩溃 |
| 调度器 | 60 s tick 循环启动（14:16:49）；`scheduler.lock` pid=3848865，最近刷新距今 21 s（14:52:49），调度器存活 |
| vault 回流 | `imported=4, errors=0, skipped_existing=317`（14:17:50） |
| memcore sweep | `archived=0, cooling=0, purged=0, lintHits=0`（14:18:16） |
| event_outbox | 切流后新增 **3** 条，全部 `delivered`；累计 4262 条全部 delivered，无 pending/dead_letter |
| Session（V3 目录格式） | 原生 526 + 根 4 = **530** 个 `.jsonl.zstd`（含子工作区目录）；切流后（≥14:16:35Z）新增 **0**，符合预期（观察期内无外部交互） |
| 兼容别名 | `deprecated_use` 累计 180（较 §3 基线 147 新增 33，均在切流前）；切流后增量 **0**；最后一条 2026-09-15T07:07:10Z（`grep_result`）；新闸口顺延至 **2026-09-22T15:07:10.543+08:00** |
| 其他单元 | 9 个 silksec/dsh 服务 + 4 个 timer 全部 active；2 条 cron 无 `# dsh-upgrade-paused` 残留 |
| 主机资源 | load 3.94（36 天运行）；内存 970Mi/16Gi；磁盘 842G 可用（12% 用量） |

**巡检结论**：所有观察期健康指标符合预期，无非预期变化、无服务退化、无崩溃。切流后 MainPID 与 §14.2 V1 一致，NRestarts 仍为 0，证据链完整。33 条新增 `deprecated_use` 均来自切流前的历史调用（最后一次 2026-09-15T07:07Z，切换在 14:16Z），切流后零增量；37 个别名观察期因此顺延至 2026-09-22，**不得提前删除**。

**U4 关账尚未执行**：等待观察期满（2026-09-18T14:16:35Z）后再按「步骤 3a→3b→3c」执行新冻结点捕获、`preserve_after_resume` 对账与恢复写者。L0–L6 未执行，不混入平台验收。
