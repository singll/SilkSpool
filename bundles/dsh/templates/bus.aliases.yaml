# ==============================================================================
# SilkSecAgent v5 兼容别名注册表（总线 owns：data/bus.aliases.yaml）
#
# 状态：**空注册表**。v5 Phase 5.2 已把所有调用方迁移到语义动词——
#   - 看板 finding 状态流转：dashboard-rpc 按 status 直达 vuln_confirm/reject/submit（无别名）
#   - 会话「登记候选漏洞」：ui-session 直达 vuln_register_candidate（actor 含 dashboard）
#   - 所有 prompt 资产（persona/skills/rules/objective）在 5.1 已改写为新动词
# 旧工具名不再注册、不再投影、不再分派；调用旧名返回 E_BUS_VERB_UNKNOWN。
#
# 别名机制（01-bus.md §3.2）仍作为通用能力保留：未来若发生跨域改名，可在此登记
# aliases（静态直通）/ dispatch_aliases（路由器分派）条目，同样过网关全管线。
# ==============================================================================
aliases: {}
dispatch_aliases: {}
