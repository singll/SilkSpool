# ==============================================================================
#  Git 忽略规则配置
#  目的: 确保敏感信息（密钥、密码、临时文件）绝对不会被提交到版本控制系统
# ==============================================================================

# --- 1. 核心凭证 (Secrets) ---
# 用户的真实配置文件，包含 IP 和账号信息
config.ini
# 任何证书文件
*.pem
*.key
*.crt

# --- 2. SSH Keys (本地存储) ---
# 忽略 keys 目录下的所有密钥文件 (私钥/公钥)
keys/*
# !例外: 保留 .gitkeep，确保别人 clone 项目时包含 keys 目录结构
!keys/.gitkeep

# --- 3. 运行时与临时文件 ---
# 脚本执行时的日志
*.log
# 脚本生成的临时 YAML 或中间脚本
*.tmp
# 本地备份下载目录
backups/
downloads/
# macOS 系统文件
.DS_Store

# --- 4. 主机特定敏感配置 ---
# 忽略 hosts 目录下所有的 .env 文件 (包含数据库密码等)
hosts/

# --- 5. IDE 配置 ---
.vscode/
.idea/

doc/
knowledge-management/